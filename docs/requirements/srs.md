# Software Requirements Specification

## Document Control

- System: `vi-history-suite`
- Version: `v0.1.0-draft`
- Owner: sole author
- Status: draft baseline

## Scope

- Purpose: deliver a governed VS Code extension baseline for content-detected VI
  history review in Git repositories, aligned to the authoritative research
  packet under `docs/research/authoritative/`.
- In scope: content detection, eligibility indexing, authoritative menu gating,
  history panel, report planning, runtime/tool detection, local CI, and
  repo-native governance artifacts.
- Out of scope for the current implemented tranche: live NI report execution,
  TimelineProvider publishable support, VS Code for Web, and external runtime
  dependencies on other VI-history repos.

## Requirements

| ID | Requirement | Rationale | Fit Criterion | Verification |
| --- | --- | --- | --- | --- |
| VHS-REQ-001 | The extension shall detect candidate LabVIEW VIs by reading bytes 8..11 and accepting `LVIN` or `LVCC`. | The product is content-based, not extension-based. | Positive and negative fixtures classify correctly. | Unit test |
| VHS-REQ-002 | The extension shall not rely on filename or file extension for VI detection. | The research paper forbids extension-based gating. | Files without `.vi` are still eligible when bytes match. | Unit and integration tests |
| VHS-REQ-003 | The extension shall support an optional stricter mode that also checks the leading `RSRC` header bytes. | The paper recommends stricter-mode mitigation against false positives. | Strict mode rejects a buffer with only offset-8 magic. | Unit test |
| VHS-REQ-004 | The extension shall contribute a `VI History` context-menu command through both `explorer/context` and `editor/title/context`. | The authoritative research calls for the command to be reachable from Explorer and editor title context menus. | The extension manifest contributes `VI History` through both menu locations. | Unit test and static inspection |
| VHS-REQ-005 | The `VI History` command shall only be visible through the authoritative menu gate `resourcePath in labviewViHistory.eligiblePaths && isWorkspaceTrusted && gitOpenRepositoryCount >= 1`. | The authoritative research defines the precise dynamic visibility gate for the first product surface. | The manifest uses that exact `when` clause for the contributed menu entries. | Unit test and static inspection |
| VHS-REQ-006 | The `VI History` command shall only be visible for tracked files that satisfy VI detection and have at least two modifying commits. | The history panel should only appear when history exists. | Eligibility context excludes files with fewer than two commits. | Unit and integration tests |
| VHS-REQ-007 | The extension shall enumerate tracked files using `git ls-files -z` or a functionally equivalent Git API-backed result. | NUL-safe tracked enumeration is the recommended baseline. | Indexer parses tracked files robustly, including paths with spaces. | Unit test |
| VHS-REQ-008 | The extension shall determine minimum eligibility history with a bounded query equivalent to `git log -n 2 --format=%H --follow -- <path>`. | The product only needs two commits to establish useful history. | Eligibility queries read at most two hashes per file. | Code review and unit test |
| VHS-REQ-009 | The extension shall treat rename-following as best effort and scope it to single-file history queries. | `--follow` is single-file and imperfect. | Documentation and code reflect best-effort rename tracking. | Documentation review |
| VHS-REQ-010 | The extension shall use partial local file reads for `file` URIs during VI detection. | Minimal I/O keeps indexing lightweight. | The local detection path reads only the bytes needed for signature checks. | Unit test and code review |
| VHS-REQ-011 | The extension shall fall back to `workspace.fs.readFile` for non-`file` URI schemes. | Remote and virtual workspaces may not expose local paths. | Detection succeeds or fails cleanly on non-file scheme stubs. | Unit test |
| VHS-REQ-012 | The extension shall gate scanning and external process execution on workspace trust. | Workspace trust is the correct safety boundary for this extension type. | Untrusted workspaces set an empty eligibility context. | Integration test and code review |
| VHS-REQ-013 | The extension shall maintain dynamic menu visibility through `setContext` object membership under `labviewViHistory.eligiblePaths`. | The authoritative research uses the `labviewViHistory.eligiblePaths` membership map as the dynamic menu gate. | The context key `labviewViHistory.eligiblePaths` is populated with eligible resource paths. | Unit test and code review |
| VHS-REQ-014 | The extension shall cache eligibility results by repository root, relative path, and `HEAD`. | Re-indexing every file on every event is wasteful. | Cache keys include repo root, file path, and HEAD. | Code review |
| VHS-REQ-015 | The extension shall bound indexing concurrency through configuration. | Large repositories require controlled background work. | The indexer respects `viHistorySuite.maxIndexedConcurrency`. | Unit test |
| VHS-REQ-016 | The extension shall open a webview history panel for the selected file. | The first review surface is a WebviewPanel. | Command execution opens a panel with file and commit metadata. | Integration test |
| VHS-REQ-017 | The history panel shall show repository name, relative path, VI signature, and commit facts. | The panel is intended for factual developer review. | Rendered HTML includes those data fields. | Unit test |
| VHS-REQ-018 | The history panel shall expose actions to open a revision, diff against the previous revision, and copy a commit hash. | These are the core review actions in the design paper. | Rendered HTML and command handlers support those actions. | Unit and integration tests |
| VHS-REQ-019 | The repo shall remain self-contained and shall not depend on `comparevi-history` or `compare-vi-cli-action`. | The new product should iterate locally without upstream churn. | No package or source dependency references those repos. | Static inspection |
| VHS-REQ-020 | The repo shall define the first real-history harness against a cloned external Git repository rather than vendored history. | Real Git history must stay external to this repo. | `HARNESS-VHS-001` is documented against `ni/labview-icon-editor`. | Documentation review |
| VHS-REQ-021 | The repo shall provide a clone-on-demand canonical harness smoke command for `HARNESS-VHS-001`. | The first live path should be runnable locally without manual repo setup. | `npm run harness:smoke` produces a factual harness report. | Smoke test |
| VHS-REQ-022 | The canonical harness smoke command shall reuse the same core history-model logic as the extension runtime. | The smoke path should validate product logic, not a parallel implementation. | Shared core modules are used by both the extension wrapper and harness smoke runner. | Static inspection and unit test |
| VHS-REQ-023 | The canonical harness smoke command shall write factual JSON, Markdown, and HTML reports under an ignored cache path. | Local review needs retained evidence without polluting tracked source. | Smoke outputs are written under `.cache/harness-reports/`. | Smoke test |
| VHS-REQ-024 | The repo shall include a real-Git unit test for the core history model using a temporary local repository. | Core history behavior should be verified without requiring VS Code runtime. | Tests create a local Git repo, commit a VI fixture twice, and assert eligibility/history. | Unit test |
| VHS-REQ-025 | The repo shall provide a VS Code extension-host integration test runner for the actual `VI History` command flow. | The extension should prove behavior in a real extension host, not only through unit tests. | `npm run test:integration` executes extension-host tests successfully. | Integration test |
| VHS-REQ-026 | The extension-host integration runner shall provision a temporary Git workspace containing both eligible and ineligible content-detected files. | Command gating must be proven against real workspace state. | The integration fixture workspace includes one two-commit VI candidate and one ineligible file, and tests assert different outcomes. | Integration test |
| VHS-REQ-027 | The history panel HTML shall expose stable semantic anchors for status, repository metadata, commit rows, and review-action controls. | Developer-facing review surfaces need deterministic structure for verification and future UX evolution. | Rendered HTML includes stable `data-testid` hooks for the status block, metadata fields, commit rows, and action controls. | Unit and integration tests |
| VHS-REQ-028 | The extension-host integration lane shall verify that the rendered history panel HTML includes factual eligibility, signature, path, and commit-subject content for an eligible file. | Opening a panel is not enough; the surfaced report must be proven to contain the expected review facts. | The real extension-host suite asserts the opened panel HTML contains the eligible file path, signature, eligibility state, and both retained commit subjects. | Integration test |
| VHS-REQ-029 | The extension-host integration lane shall verify `copyHash`, `openCommit`, and `diffPrevious` behavior through the real panel message handler. | Core review actions must be proven on the same action path used by the webview surface. | The real extension-host suite dispatches those three actions through the captured panel handler and asserts clipboard or Git/diff outcomes. | Integration test |
| VHS-REQ-030 | The test API shall retain factual summaries of the last panel action outcome for review-action verification. | Integration evidence should report what action was handled without depending on incidental UI state alone. | The exported extension test API returns the last action summary and action count after dispatched panel actions. | Integration test |
| VHS-REQ-031 | The history panel shall include a chronology packet that states retained revision count plus newest and oldest retained commit summaries. | Developers need immediate chronology cues without parsing the full table first. | Rendered HTML includes anchored chronology fields for order, retained revision count, newest commit summary, and oldest commit summary. | Unit and integration tests |
| VHS-REQ-032 | The history table shall show explicit compare-base context for each retained commit row. | Commit-to-commit review should not require inferring which prior revision a diff action targets. | Each rendered commit row includes a compare-base field showing the previous retained hash or an oldest-revision marker. | Unit and integration tests |
| VHS-REQ-033 | The history panel shall state factual binary-review limitations for Git-backed LabVIEW VI revisions. | The review surface should explain what the panel proves and what still depends on external tooling. | Rendered HTML includes a retained note that VI revisions are binary artifacts and that open/diff actions delegate to VS Code handlers and installed tooling. | Unit and integration tests |
| VHS-REQ-034 | The history table shall retain an explicit selected-versus-base pairing for rows that can diff against a previous retained revision. | Reviewers should not have to infer which revision pair a `Diff prev` action will use. | Rendered HTML includes a pairing element that states the selected retained hash and the retained base hash for diffable rows. | Unit and integration tests |
| VHS-REQ-035 | The history panel shall retain reviewer guidance for using chronology facts, compare pairs, and binary-review actions safely. | The review surface should help developers understand how to use the packet without overclaiming what the panel itself proves. | Rendered HTML includes a guidance block with retained steps covering chronology review, compare-pair interpretation, and binary-review escalation. | Unit and integration tests |
| VHS-REQ-036 | The history panel shall retain a confidence-and-scope packet with stable anchors. | Reviewers need an explicit place to judge what the current surface proves and where its boundaries are. | Rendered HTML includes a retained confidence-and-scope block with stable verification hooks. | Unit and integration tests |
| VHS-REQ-037 | The confidence-and-scope packet shall state the direct local evidence basis and the review scope included in this surface. | Confidence claims should be grounded in concrete evidence sources, not vague sentiment. | Rendered HTML states that the packet is based on local Git history, tracked-file status, and content-detected VI signature checks, and that it directly covers chronology, path provenance, retained hashes, and command routing. | Unit and integration tests |
| VHS-REQ-038 | The confidence-and-scope packet shall state which review questions remain outside scope without external binary comparison tooling. | The panel should fail closed on binary semantic and cosmetic claims it cannot prove by itself. | Rendered HTML states that binary semantic differences, visual/cosmetic change detection, and NI comparison-report output require external comparison tooling. | Unit and integration tests |
| VHS-REQ-039 | The history panel shall expose a panel-level `Copy review packet` action. | Reviewers need a portable artifact they can paste into human review flows without recreating the packet manually. | Rendered HTML includes a panel-level copy action that dispatches without requiring a commit hash. | Unit and integration tests |
| VHS-REQ-040 | The `Copy review packet` action shall copy a factual plain-text packet grounded in the same local review evidence shown in the panel. | The portable artifact should preserve the panel’s governed facts rather than invent a separate summary path. | The copied text includes repository, path, signature, retained revision count, confidence/scope statements, and retained compare pairs from the current review packet. | Unit and integration tests |
| VHS-REQ-041 | The repo shall provide a single-command local design gate for the governed development baseline. | The continuous design gate should self-propel development instead of relying on remembered manual command sequences. | `npm run design:gate` executes successfully and writes retained design-gate reports under `.cache/design-gate/`. | Unit test and gate run |
| VHS-REQ-042 | The local design gate shall run unit coverage, extension-host integration, canonical harness smoke, and standards assurance sequentially. | The repo should fail closed unless both product behavior and governed assurance pass in one lane. | The design-gate plan includes those four ordered steps and the live gate run records them. | Unit test and gate run |
| VHS-REQ-043 | The local design gate shall retain machine-readable and Markdown reports for the latest run. | A self-propelling gate needs durable local evidence for later review, not just terminal output. | The gate writes `latest-report.json` and `latest-report.md` under `.cache/design-gate/`, including step status and retained assurance summary when available. | Unit test and gate run |
| VHS-REQ-044 | The local design gate shall retain the weakest covered source files from the latest unit-coverage summary. | A self-propelling gate should derive the next development focus from retained evidence, not operator memory. | The retained gate report records weakest source files ordered deterministically by line percentage, then larger covered-total scope, then relative path. | Unit test and gate run |
| VHS-REQ-045 | The local design gate shall surface a retained next-focus cue derived from the weakest retained source-coverage fact. | The autonomous lane should identify the next improvement target deterministically. | The retained gate report and Markdown summary state the weakest source file as the next focus. | Unit test and gate run |
| VHS-REQ-046 | The local design gate shall state an explicit reason when coverage-focus facts are unavailable. | Missing coverage evidence should fail closed in the report instead of disappearing silently. | When the coverage summary cannot be read or yields no source entries, the retained report states a machine-readable unavailable reason. | Unit test |
| VHS-REQ-047 | The local design gate orchestration shall live in a reusable shared runner that is invoked by the CLI entrypoint. | Self-propelling development should be testable below the CLI shell boundary. | The CLI entrypoint delegates to a shared runner module that can be unit-tested without spawning the real gate. | Static inspection and unit test |
| VHS-REQ-048 | The local design gate shall retain a fail report when a gated step stops the sequence before completion. | Autonomous development needs durable failure evidence, not only successful-run records. | When a step fails, the retained JSON and Markdown reports still record the failing status, completed steps, and derived next-focus data when available. | Unit test |
| VHS-REQ-049 | The `VI History` command shall show an informational message and stop cleanly when invoked without a selected resource and without an active editor target. | Silent no-ops weaken developer experience and obscure why no review packet appeared. | Invoking the command with no URI target and no active editor shows a user-facing informational message and does not create a panel. | Unit test |
| VHS-REQ-050 | The `VI History` command shall show a warning and stop without loading history when the workspace is untrusted. | Workspace-trust gating should be explicit to the developer, not only implicit in missing behavior. | In an untrusted workspace, the command shows a warning and does not call the history loader or open a panel. | Unit test |
| VHS-REQ-051 | The `VI History` command shall show an informational message and stop without loading history when the selected file is not currently eligible. | Ineligible-file handling should explain why the review surface was not opened. | For an ineligible file, the command shows an informational message and does not call the history loader or open a panel. | Unit test |
| VHS-REQ-052 | History-panel actions that require Git-backed revision URIs shall show a warning and retain a `missing-git-uri` action outcome when the Git URI cannot be resolved. | Review-action failures should be actionable for the developer and explicit in retained evidence. | `openCommit` and `diffPrevious` retain `missing-git-uri` and show a warning when Git-backed revision URIs are unavailable. | Unit test |
| VHS-REQ-053 | The `diffPrevious` panel action shall show an informational message and retain a `missing-previous-hash` outcome when no retained base revision exists. | Reviewers need explicit guidance when a retained row cannot be diffed against a previous revision. | Dispatching `diffPrevious` for a commit without `previousHash` shows an informational message, records `missing-previous-hash`, and does not execute a diff command. | Unit test |
| VHS-REQ-054 | History-panel action handling shall ignore unsupported or hashless action messages without executing Git, open, or diff side effects, while retaining an explicit tracker outcome. | The command layer should fail closed on malformed or unsupported panel traffic. | Hashless messages retain `ignored-missing-hash`, unsupported messages retain `unsupported-command`, and neither path executes Git/open/diff side effects. | Unit test |
| VHS-REQ-055 | The harness smoke CLI shall default to `HARNESS-VHS-001` when no harness identifier is supplied. | The canonical live harness should remain the deterministic default entrypoint. | Running the CLI with no arguments resolves `HARNESS-VHS-001` as the selected harness. | Unit test |
| VHS-REQ-056 | The harness smoke CLI shall accept `--harness-id` and `--strict-rsrc-header` as explicit governed options. | Local harness iteration needs a small stable option surface for selecting a harness and detection strictness. | Parsing the CLI arguments retains the selected harness id and strict-header flag in the harness-run request. | Unit test |
| VHS-REQ-057 | The harness smoke CLI shall print deterministic success lines for retained JSON, Markdown, HTML, eligibility, signature, and commit-count facts. | Developers need a stable command-line summary that aligns with the retained report artifacts. | A successful CLI run writes those summary lines in a deterministic order. | Unit test |
| VHS-REQ-058 | The harness smoke CLI shall expose a reusable runner below the shell boundary. | The harness entrypoint should be directly testable without spawning a separate Node process. | The CLI shell entrypoint delegates to an exported reusable runner function. | Static inspection and unit test |
| VHS-REQ-059 | The harness smoke CLI shall reject unknown arguments or a missing `--harness-id` value with usage guidance. | Silent acceptance of malformed CLI input is a weak design for a governed harness surface. | Invalid arguments throw a usage-bearing error before the harness run begins. | Unit test |
| VHS-REQ-060 | The harness smoke CLI shall support `--help` and exit cleanly without running the harness. | Operators need a deterministic way to discover the governed option surface without side effects. | `--help` returns a help status, prints usage text, and does not invoke the harness runner. | Unit test |
| VHS-REQ-061 | The VI history service shall select the most specific matching Git repository root when multiple Git API repositories prefix the target path. | Overlapping repository roots are a real weak-design seam for nested repositories. | Repository-root resolution chooses the longest matching Git API root for the target path. | Unit test |
| VHS-REQ-062 | The VI history service shall fall back to Git CLI repository-root resolution when no Git API repository matches the target path. | The service must remain functional when the Git extension has no matching repository context. | If no Git API match exists, the service resolves the repository root via `git rev-parse --show-toplevel` on the file’s parent directory. | Unit test |
| VHS-REQ-063 | The VI history service shall forward `strictRsrcHeader` and `maxHistoryEntries` settings to the shared history-model loader. | Service-level configuration should deterministically shape the loaded review packet. | The load path passes the current configuration values into the shared history-model loader options. | Unit test |
| VHS-REQ-064 | The VI history service shall expose Git-URI translation only through the available Git API. | The service should not synthesize Git revision URIs outside the Git extension contract. | `toGitUri` delegates to the Git API when present and returns `undefined` otherwise. | Unit test |
| VHS-REQ-065 | The URI-based VI magic wrapper shall delegate `file` URIs to the file-system probe helper. | Local file probes should continue to use the bounded file-read path instead of the workspace-fs fallback. | `readViProbeBytes` calls the file-system probe helper for `file` URIs and does not call `workspace.fs.readFile` in that path. | Unit test |
| VHS-REQ-066 | The URI-based VI magic wrapper shall truncate non-`file` workspace-fs reads to the minimum detection-header length. | Remote and virtual workspace reads should retain only the bytes required for signature detection. | `readViProbeBytes` returns only the first detection-header bytes from `workspace.fs.readFile` for non-`file` URIs. | Unit test |
| VHS-REQ-067 | The URI-based VI magic wrapper shall fail closed when probe reads throw. | Detection wrappers should return `undefined` or `false` rather than surfacing probe exceptions as product behavior. | `detectViSignatureFromUri` returns `undefined` and `isLabviewViByMagic` returns `false` when probe reads fail. | Unit test |
| VHS-REQ-068 | The built-in Git API resolver shall return `undefined` when the `vscode.git` extension is not installed. | The extension should degrade gracefully when the built-in Git extension is unavailable. | `getBuiltInGitApi()` returns `undefined` without attempting activation when `vscode.extensions.getExtension('vscode.git')` returns no extension. | Unit test |
| VHS-REQ-069 | The built-in Git API resolver shall request API version `1` from the active or activated `vscode.git` extension. | The repo should use a stable, governed API version rather than an implicit extension contract. | `getBuiltInGitApi()` returns the value from `getAPI(1)` and activates the Git extension only when it is not already active. | Unit test |
| VHS-REQ-070 | The built-in Git API resolver shall return `undefined` when the Git extension exports do not provide `getAPI`. | The product should fail closed rather than assuming an incompatible Git extension export surface. | `getBuiltInGitApi()` returns `undefined` when the active or activated extension exports lack a callable `getAPI`. | Unit test |
| VHS-REQ-071 | The built-in Git API resolver shall fail closed when Git extension activation throws. | Git extension activation failure should not crash the product’s local review path. | `getBuiltInGitApi()` returns `undefined` when `extension.activate()` throws. | Unit test |
| VHS-REQ-072 | The eligibility indexer shall clear retained eligible-path state and indexed repository roots when the workspace is untrusted. | Trust gating should remove stale review visibility instead of leaving previously indexed state behind. | An untrusted refresh results in empty eligible-path context and an empty indexed-root snapshot. | Unit test |
| VHS-REQ-073 | The eligibility indexer shall register repository state listeners for repositories present at startup and for repositories opened after startup, and dispose those listeners when repositories close. | Repository change tracking should remain correct as the Git repository set changes over time. | The indexer attaches state listeners to existing and newly opened repositories, and disposes the matching listener on repository close. | Unit test |
| VHS-REQ-074 | The eligibility indexer shall fail closed on repository-level Git enumeration errors without aborting the full refresh. | One broken repository should not prevent eligibility updates for other repositories. | If tracked-file or HEAD queries fail for one repository, the refresh still completes and indexes other repositories. | Unit test |
| VHS-REQ-075 | The eligibility indexer shall fail closed on per-file eligibility evaluation errors without aborting the full repository refresh. | One unreadable or malformed file should not prevent eligibility updates for the rest of the repository. | If eligibility evaluation throws for one tracked file, the refresh still completes and only successful eligible files appear in the retained context. | Unit test |
| VHS-REQ-076 | The eligibility indexer shall reuse cached eligibility results for the same repository root, normalized relative path, and HEAD. | Repeated refreshes on the same Git state should avoid re-evaluating unchanged files. | Two refreshes with the same repository root, relative path, and HEAD evaluate a file only once. | Unit test |
| VHS-REQ-077 | The eligibility indexer shall invalidate cached eligibility results when the repository HEAD changes. | Eligibility should be recomputed when the underlying Git state changes. | A refresh with a different HEAD re-evaluates tracked files even if the relative path is the same. | Unit test |
| VHS-REQ-078 | The eligibility indexer shall expose normalized URI path variants in its dynamic context-key set. | Dynamic menu visibility should match equivalent path spellings used by VS Code and Git-backed URIs. | The context-key helper includes original, normalized, and slash-normalized path variants for a URI. | Unit test |
| VHS-REQ-079 | The design-gate runner shall create the retained design-gate report directory recursively before writing latest JSON and Markdown report artifacts. | A self-propelling gate should not rely on a precreated cache directory to retain its evidence. | Persisting a design-gate report first creates `.cache/design-gate/` recursively, then writes `latest-report.json` and `latest-report.md`. | Unit test |
| VHS-REQ-080 | The design-gate runner shall retain spawned-step stdout, stderr, and duration while streaming the same bytes to the active operator output channels. | Autonomous gate debugging needs retained evidence and live operator visibility from the same execution facts. | The shared spawned-step executor returns accumulated stdout, stderr, and duration while forwarding emitted bytes to the configured stdout/stderr sinks. | Unit test |
| VHS-REQ-081 | The design-gate runner shall fail closed when a spawned gate step emits a process error or closes without an exit code. | The autonomous lane should not treat startup failures or ambiguous process termination as a successful gate step. | Spawn errors resolve to exit code `1` with retained error text, and `close(null)` is coerced to exit code `1`. | Unit test |
| VHS-REQ-082 | The extension shall register its primary command under the authoritative command identifier `labviewViHistory.open`. | The authoritative research uses `labviewViHistory.open` as the product command identity for menu wiring and activation. | The manifest and activation events declare `labviewViHistory.open` and the extension registers that command at runtime. | Unit test and static inspection |
| VHS-REQ-083 | The extension manifest shall declare `vscode.git` as an extension dependency and activate through `onStartupFinished` plus `onCommand:labviewViHistory.open`. | The authoritative research directs the extension to depend on the built-in Git extension and avoid startup-heavy eager activation. | The manifest includes `extensionDependencies: ["vscode.git"]`, `onStartupFinished`, and `onCommand:labviewViHistory.open`. | Unit test and static inspection |
| VHS-REQ-084 | The extension manifest shall declare limited untrusted-workspace support and restrict external LabVIEW tool configuration in untrusted workspaces. | The authoritative research directs the product to use VS Code workspace-trust declarations in addition to runtime trust checks. | The manifest declares `capabilities.untrustedWorkspaces.supported = limited` with a description and restricted external-tool configuration keys. | Unit test and static inspection |
| VHS-REQ-085 | The published review surface shall remain WebviewPanel-based, and TimelineProvider shall remain outside the published extension path while it is a proposed API. | The authoritative research explicitly treats TimelineProvider as experimental and non-publishable. | Architecture records state that the published extension uses webview-based review surfaces and does not depend on TimelineProvider. | ADR review |
| VHS-REQ-086 | The indexing experience shall report percent, processed items, and ETA through `window.withProgress`. | The authoritative research specifies a richer progress surface than the current message-only progress. | Indexing progress messages include percent, processed/total, and ETA estimates in the notification progress lane. | Integration test |
| VHS-REQ-087 | The extension shall surface a discreet status-bar progress item for background indexing. | The authoritative research calls for a secondary progress surface that stays visible without blocking the user. | A status-bar item appears during indexing with spinner, percent, processed/total, and ETA, and hides when indexing completes. | Integration test |
| VHS-REQ-088 | The webview review surface shall expose progress updates for long-running report-generation operations. | The authoritative research calls for webview-visible progress feedback during report generation. | The webview handles report-generation progress messages and renders a progress strip or equivalent anchored surface. | Integration test |
| VHS-REQ-089 | Report generation shall verify that both selected revision blobs are LabVIEW VIs by content before invoking external comparison tooling. | The authoritative research requires blob verification as a precondition to compare/report execution. | The report-generation path rejects non-VI revision blobs before tool invocation and retains the failure reason. | Unit and integration tests |
| VHS-REQ-090 | Generated HTML comparison reports shall use the filename pattern `{type}-report-{fullFilename}.html`. | The authoritative research defines the report artifact naming contract explicitly. | Generated HTML report paths preserve the full filename and the requested `{type}-report-` prefix. | Unit and integration tests |
| VHS-REQ-091 | Report generation shall extract same-name revisions to distinct temporary filenames before invoking LVCompare-compatible tooling. | The authoritative research identifies same-name revision comparison as a high-risk LVCompare limitation. | Temporary extracted base/head files use distinct filenames and do not collide when both revisions share the same original filename. | Unit and integration tests |
| VHS-REQ-092 | Report artifacts and report metadata shall be stored under workspace-scoped `context.storageUri` directories. | The authoritative research directs the extension to use workspace storage for generated reports and related metadata. | The report-generation path stores extracted revisions, HTML output, and a small metadata record under `context.storageUri`. | Integration test |
| VHS-REQ-093 | The review surface shall show open-report links only when the generated HTML exists, and shall expose report resources through `asWebviewUri` plus narrow `localResourceRoots`. | The authoritative research requires existence-based report visibility and secure webview resource exposure. | Report links are absent when HTML does not exist and present only through webview-safe URIs rooted in explicitly allowed local resource paths. | Unit and integration tests |
| VHS-REQ-094 | Runtime tool settings shall allow explicit user override through `labviewCliPath`, `lvComparePath`, `labviewExePath`, and `preferBitness`, with user configuration taking precedence over discovery and failing closed when an explicit configured path is missing. | The authoritative research prioritizes explicit user configuration ahead of heuristic tool discovery, and runtime configuration should not silently drift to a different tool when a configured path is invalid. | Settings exist for those tool surfaces, configured paths are checked before auto-detection, and an invalid configured path yields a stable blocked reason. | Unit tests |
| VHS-REQ-095 | Windows runtime discovery shall probe registry views and documented installation roots to locate LabVIEW 2026 Q1 executables and comparison tooling. | The authoritative research defines Windows as the richest supported discovery surface and names registry/install-root probing as the preferred strategy. | Discovery logic retains Windows registry probe plans, parses registry-discovered LabVIEW executables, scans documented install roots, and returns classified host-native candidates. | Unit tests |
| VHS-REQ-096 | macOS and Linux runtime discovery shall scan documented install roots and retain explicit platform availability constraints. | The authoritative research requires cross-platform notes and explicit handling of constrained NI platform support. | Discovery logic scans documented Linux roots, returns a stable unsupported constraint for macOS 2026 Q1, and retains explicit blocked reasons when tooling is unavailable. | Unit tests and documentation review |
| VHS-REQ-097 | The extension shall remain desktop/remote-host oriented and shall not target a publishable VS Code web surface while child-process Git and LabVIEW tooling remain required. | The authoritative research explicitly treats web support as out of target scope for this product shape. | Product and architecture records state that the published extension targets desktop or remote extension hosts only. | ADR and documentation review |
| VHS-REQ-098 | Packaging guidance shall support `vsce package`, local VSIX installation, and documented Marketplace publish prerequisites. | The authoritative research includes a governed packaging and release path for the extension. | Repo documentation defines package, local install, and publish prerequisites for VS Code Marketplace delivery. | Documentation review |
| VHS-REQ-099 | The repo shall provide a pure comparison-report planning module before live LabVIEW report execution is introduced. | The authoritative second-round research recommends isolated report-generation modules so report naming, storage, and engine selection can be governed independently of runtime execution. | A pure module derives report artifact layout and command plans without invoking external tools. | Unit test and ADR review |
| VHS-REQ-100 | The comparison-report planning module shall derive the exact report filename pattern `{type}-report-{fullFilename}.html`. | The authoritative research defines the report artifact naming contract explicitly. | The planning module returns exact filenames such as `diff-report-foo.vi.html` and `print-report-foo.vi.html`. | Unit test |
| VHS-REQ-101 | The comparison-report planning module shall derive distinct staged filenames for left and right revisions of the same VI basename. | The authoritative research identifies same-name revision handling as a hard comparison constraint. | The planning module generates different staged left/right filenames even when both revisions share the same original basename. | Unit test |
| VHS-REQ-102 | The comparison-report planning module shall derive deterministic workspace-storage paths under `context.storageUri/reports/<repoId>/<fileId>/...` plus narrow local-root candidates for later webview exposure. | The authoritative research directs report artifacts to workspace storage and recommends narrow local resource roots. | The planning module derives stable report/staging/metadata paths plus allowed local-root paths from a storage root, repository root, and repo-relative file path. | Unit test |
| VHS-REQ-103 | The primary comparison-report command plan shall target `LabVIEWCLI -OperationName CreateComparisonReport` with `HTMLSingleFile` as the default report format. | The authoritative research names `CreateComparisonReport` as the primary programmatic engine and strongly prefers `HTMLSingleFile`. | The planning module returns an exact primary command plan using `CreateComparisonReport`, `-reportType HTMLSingleFile`, and the requested report path. | Unit test |
| VHS-REQ-104 | The fallback comparison-report command plan shall target `LVCompare` with an optional explicit `-lvpath`. | The authoritative research treats `LVCompare` as the interactive fallback and documents `-lvpath` for explicit LabVIEW selection. | The planning module returns the LVCompare executable plus left/right VI paths and appends `-lvpath` only when an explicit LabVIEW path is supplied. | Unit test |
| VHS-REQ-105 | The core history model shall auto-discover the Git repository root from the target filesystem path when `repoRoot` is not supplied. | The shared history path should work from file selection alone and not require callers to pre-resolve the repository root. | Loading eligibility or history from a tracked VI path without `repoRoot` derives the correct repository root and repo-relative path. | Unit test |
| VHS-REQ-106 | The core history model shall preserve the default non-strict VI signature mode and default history limit when optional overrides are omitted. | The shared model should expose the product's default behavior without forcing callers to restate default options. | A tracked file with offset-8 VI magic but no strict `RSRC` header is accepted in default mode, and loading history without `historyLimit` returns the available commit chain with `previousHash` only on rows that have a predecessor. | Unit test |
| VHS-REQ-107 | The design-gate runner shall support default filesystem-backed coverage reads and report persistence when no injected I/O helpers are supplied. | The continuous gate should work as a real repo-local tool, not only through injected test doubles. | A runner call against a temporary repo writes retained reports under `.cache/design-gate/` and reads coverage focus from an on-disk `coverage/coverage-summary.json` without injected file helpers. | Unit test |
| VHS-REQ-108 | The spawned-step executor shall retain the first terminal result and ignore later child-process `error` or `close` events after settlement. | The gate should fail deterministically even when child-process event ordering is noisy. | After a spawned step settles, later `error` or `close` events do not alter the retained exit code, stdout, stderr, or duration. | Unit test |
| VHS-REQ-109 | The canonical harness registry shall fail closed for unknown harness identifiers with a stable error message. | The governed harness layer should not silently invent or coerce unsupported harness IDs. | Resolving an unknown harness ID throws `Unknown harness id: <id>`. | Unit test |
| VHS-REQ-110 | The design-gate CLI script entrypoint shall route the retained `runDesignGateCliMain` exit code into `process.exitCode` without forcing an immediate process exit. | The CLI should remain script-friendly and testable while preserving deterministic shell status. | A script-mode helper applies a supplied exit code to a writable `process`-like target, and the top-level module branch uses that helper. | Unit test and static inspection |
| VHS-REQ-111 | The harness-smoke CLI script entrypoint shall route retained success or failure into `process.exitCode` and a stable stderr message without forcing an immediate process exit. | The governed harness CLI should match the deterministic, script-friendly behavior of the design-gate CLI. | A script-mode helper returns `false` when not running as main, returns `true` when active, writes a stable error line on failure, and applies the resulting exit code to a writable `process`-like target. | Unit test and static inspection |
| VHS-REQ-112 | The Git CLI helper layer shall return trimmed repository HEAD, repository root, and tracked-file facts from a real temporary Git repository. | The wrapper functions around `git rev-parse` and `git ls-files -z` should be proven against real Git behavior, not only parser stubs. | A unit test initializes a temporary Git repo, commits tracked files, and verifies `getRepoHead`, `getRepoRoot`, and `listTrackedFiles` against the real repo state. | Unit test |
| VHS-REQ-113 | The Git CLI helper layer shall return bounded commit hashes and structured history entries for a tracked file from a real temporary Git repository. | The bounded-history and structured-history wrappers should be proven against real Git behavior, not only via higher-level consumers. | A unit test commits a tracked file multiple times, then verifies `getFileCommitHashes` respects the requested limit and `getFileHistoryEntries` returns ordered history records with hash, author date, author name, and subject. | Unit test |
| VHS-REQ-114 | The Git CLI helper layer shall fail closed on Git subprocess errors and shall fall back to the bare `git` executable when no override or Windows candidate path is available. | The Git runtime surface should remain deterministic both when invocation fails and when no explicit Windows installation path can be resolved. | A unit test proves `resolveGitExecutable` returns `git` when no override/candidate exists, and `runGit` rejects when Git returns a subprocess error. | Unit test |
| VHS-REQ-115 | The filesystem VI-probe helper shall return only the bytes actually read, up to the minimum detection-header length. | The file-backed probe path should remain bounded and should not fabricate bytes beyond the real file contents. | Reading a short file returns exactly the bytes present, and reading a longer file returns only the minimum header-length probe window. | Unit test |
| VHS-REQ-116 | The filesystem VI-signature detector shall fail closed to `undefined` when the probe read cannot be completed. | The direct filesystem detection layer should not leak file I/O failures as uncaught exceptions. | Probing a missing or unreadable file resolves to `undefined` instead of throwing. | Unit test |
| VHS-REQ-117 | The eligibility indexer shall debounce workspace-folder and workspace-trust refresh triggers, and it shall tolerate startup with no Git API. | The indexer should avoid redundant refresh storms and should still function in CLI-only or Git-extension-unavailable environments. | Starting without a Git API still registers workspace listeners, and multiple rapid listener triggers schedule only one additional refresh after the debounce window. | Unit test |
| VHS-REQ-118 | The eligibility indexer shall ignore duplicate repository listener registrations and repositories that do not expose a state-change emitter. | Repository listener management should fail closed instead of stacking duplicate listeners or assuming incomplete Git API surfaces. | Opening the same repository twice or a repository without `state.onDidChange` does not create duplicate listener registrations. | Unit test |
| VHS-REQ-119 | The eligibility indexer context-key helper shall ignore empty values and add lowercase path variants on Windows. | Dynamic menu gating should be robust across empty inputs and Windows path-case behavior. | Empty `fsPath`/`path` values yield no context keys, and Windows mode includes lowercase filesystem and slash-normalized variants. | Unit test |
| VHS-REQ-120 | The eligibility indexer concurrent worker loop shall stop cleanly when workers outnumber remaining queue items. | Queue exhaustion should not create undefined-worker side effects or hangs under higher configured concurrency. | `forEachConcurrent` processes each value once and resolves cleanly when more workers exist than queued items. | Unit test |
| VHS-REQ-121 | The comparison-report staging planner shall fall back to deterministic `left-` and `right-` filename prefixes when revision identifiers are omitted or blank. | The report subsystem still needs stable staged filenames before revision hashes are available. | A staged revision plan without usable revision identifiers produces `left-<filename>` and `right-<filename>` paths. | Unit test |
| VHS-REQ-122 | The comparison-report planning subsystem shall fail closed on empty required identifiers and paths instead of producing malformed artifact or command plans. | Empty planning inputs are a weak design for a governed report-generation contract. | Public planning helpers throw stable non-empty-field errors when required storage roots, filenames, or VI/report paths are blank. | Unit test |
| VHS-REQ-123 | The history panel and copied review packet shall render an explicit `No retained commits` fallback when the current model has no retained commits. | The review surface should stay truthful and readable even when history facts cannot currently populate newest/oldest summaries. | HTML and plain-text packet output render `No retained commits` for newest/oldest summaries when `commits` is empty. | Unit test |
| VHS-REQ-124 | The design-gate runner shall retain the coverage-focus unavailable reason and omit the retained `nextFocus` cue when no governed source coverage facts are available. | The autonomous gate should not invent a next tranche when the retained source-coverage evidence is unavailable. | A retained gate report with an unavailable coverage-focus reason leaves `nextFocus` undefined and preserves the unavailable reason in JSON and Markdown outputs. | Unit test |
| VHS-REQ-125 | The spawned design-gate step runner shall derive step duration from the default wall clock when no injected time source is provided. | The governed step runner should remain deterministic in production use even when tests are not injecting a synthetic clock. | A spawned step using the default clock path records duration from `Date.now()` and retains streamed stdout/stderr facts. | Unit test |
| VHS-REQ-126 | The harness-smoke runner shall stamp generated reports from the default ISO timestamp path when no injected clock is provided. | The retained harness report should stay deterministic in production use even when tests are not injecting a synthetic time source. | A harness-smoke run without a supplied `now` dependency records `generatedAt` from the default `new Date().toISOString()` path and persists the same timestamp into JSON output. | Unit test |
| VHS-REQ-127 | The comparison-report subsystem shall derive revision-blob Git specifiers as `<revision>:<normalized/relative/path>` before report preflight or staging. | Report-generation preflight should use a deterministic Git object selector instead of ad hoc path assembly. | A normalized relative path such as `Tooling\deployment\foo.vi` yields a blob specifier like `abcdef123456:Tooling/deployment/foo.vi`. | Unit test |
| VHS-REQ-128 | The comparison-report subsystem shall verify both selected revision blobs as content-detected VIs before compare/report generation is considered ready. | The authoritative research requires both revisions to be proven VI blobs before any LVCompare or LabVIEWCLI report flow proceeds. | A preflight run against two Git revisions reports `ready: true` only when both selected blobs resolve to `LVIN` or `LVCC` by content detection. | Unit test |
| VHS-REQ-129 | The comparison-report subsystem shall fail closed with explicit side-specific blocked reasons when a selected revision blob cannot be read or is not a VI. | Report generation should not proceed on ambiguous or invalid revision inputs. | A preflight run reports `left-blob-not-vi`, `left-blob-read-failed`, `right-blob-not-vi`, or `right-blob-read-failed` when either selected blob is invalid or unreadable. | Unit test |
| VHS-REQ-130 | The local design gate shall suppress the coverage-based `nextFocus` cue when all governed source files have reached 100% line coverage. | A 100%-covered tie should not masquerade as a real next file-level improvement target. | The retained gate report omits `nextFocus` when the weakest retained source entry is already at 100.0% line coverage. | Unit test and gate run |
| VHS-REQ-131 | The local design gate shall derive the next product tranche from a governed development queue when line coverage is saturated. | The self-propelling gate should continue driving implementation after the line-coverage baseline is complete. | When the weakest retained source coverage fact is 100.0%, the retained gate report records the first active or queued tranche from `docs/product/development-queue.json`. | Unit test and gate run |
| VHS-REQ-132 | The local design gate shall retain an explicit unavailable reason when the governed development queue cannot yield an active or queued tranche. | Missing product-queue guidance should fail closed instead of silently dropping the next tranche signal. | Reading an empty or unavailable development queue records a stable unavailable reason in the retained report. | Unit test |
| VHS-REQ-133 | The VI History panel shall expose `Generate report` only for retained revisions that have a retained base revision and shall keep the action disabled for the oldest retained revision. | Comparison report generation is pair-based and should not imply that a base-less oldest revision can produce a compare report. | Rows with `previousHash` render `Generate report`; rows without `previousHash` render the action disabled. | Unit and integration test |
| VHS-REQ-134 | The comparison-report action shall fail closed when `context.storageUri` is unavailable and shall inform the user that workspace-scoped extension storage is required. | The authoritative storage contract depends on workspace-scoped storage and should not silently fall back to temp or repo paths. | Triggering `Generate report` without workspace storage yields a stable warning and records the missing-storage outcome. | Unit test |
| VHS-REQ-135 | The comparison-report action shall persist a retained packet artifact and metadata under `context.storageUri/reports/<repoId>/<fileId>/`, while reserving `{type}-report-{fullFilename}.html` as the governed NI-generated report output path. | The authoritative report filename must remain available for actual NI output instead of being consumed by a placeholder packet surface. | Triggering `Generate report` writes a retained packet artifact, `report-metadata.json`, and staged left/right filenames under the workspace-scoped reports root, while the governed `diff-report-<fullFilename>.html` path remains the runtime output target. | Unit and integration test |
| VHS-REQ-136 | The comparison-report action shall surface the retained packet artifact through a dedicated webview panel using `asWebviewUri` and `localResourceRoots` limited to the workspace storage root and repo-specific report root. | Report artifacts should be surfaced through a least-privilege webview path consistent with the authoritative research. | The comparison-report panel opens with `localResourceRoots` restricted to workspace storage and the repo-specific report root and loads the retained packet artifact through an `asWebviewUri` iframe. | Unit test |
| VHS-REQ-137 | The retained comparison-report packet shall state truthful runtime-execution status and retain preflight, runtime-selection, and execution facts whether or not actual NI HTML report generation succeeds. | The stored packet must remain truthful after live NI runtime execution is introduced. | Stored packet HTML and metadata record `runtimeExecutionState`, readiness facts, execution failure reasons when present, and whether the governed report output currently exists. | Unit and integration test |
| VHS-REQ-138 | The report-generation architecture shall preserve a runtime-provider boundary for Windows 64-bit execution so an isolated LabVIEW 2026 Q1 Windows container path can be introduced without colliding with an already-open host-native 64-bit LabVIEW session. | Author direction requires the future 64-bit runtime path to avoid host-session collisions while keeping the current repo self-contained and explicit about provider choice. | Architecture records state that Windows 64-bit report execution remains behind a provider boundary, with host-native selection implemented now and isolated container execution reserved as the preferred future isolation path. | ADR and documentation review |
| VHS-REQ-139 | The comparison-report action shall resolve the governed runtime/tool selection before persisting a report packet and shall retain that runtime summary with the selected revision pair. | Runtime detection should inform the stored report surface now, even before live NI execution is wired. | Triggering `Generate report` records the selected provider, engine, and runtime blocked reason or notes in the stored packet metadata. | Unit and integration tests |
| VHS-REQ-140 | The retained comparison-report packet shall distinguish `blocked-runtime` from `blocked-preflight` and shall record runtime-selection facts when NI report execution cannot currently proceed. | Preflight validity and runtime availability are separate truths and should not be collapsed into one blocked state. | A packet with valid VI blobs but unavailable tooling records `reportStatus: blocked-runtime`, `runtimeExecutionState: not-available`, and the runtime selection summary. | Unit and integration tests |
| VHS-REQ-141 | The stored comparison-report HTML shall render the runtime provider, engine, blocked reason, selected tool paths, and retained runtime notes. | The HTML packet is part of the product surface and should expose runtime readiness facts without requiring metadata inspection. | Rendered report HTML includes a runtime-selection section with provider, engine, blocked reason, tool paths, and runtime notes. | Unit test |
| VHS-REQ-142 | The repo shall provide a pure execution-plan builder that maps a ready retained report packet to the exact LabVIEW CLI or LVCompare command invocation without executing external tooling. | The active runtime-execution tranche needs a governed bridge from retained packet facts to concrete tool invocation, while keeping the first slice static and testable. | A pure module accepts a retained packet and returns either a blocked reason or a fully specified executable-plus-args command plan. | Unit test |
| VHS-REQ-143 | The execution-plan builder shall fail closed when the retained packet is `blocked-preflight`, `blocked-runtime`, or lacks the tool-specific fields required by the selected runtime engine. | Runtime execution should not start from ambiguous or incomplete packet state. | The execution-plan builder returns stable blocked reasons for preflight/runtime blocks and incomplete CLI/LVCompare selections. | Unit test |
| VHS-REQ-144 | A ready host-native LabVIEW CLI runtime selection shall map to a `CreateComparisonReport` command plan with `HTMLSingleFile`, explicit staged VI paths, explicit output path, `-Headless`, and explicit `-LabVIEWPath`. | The authoritative research makes LabVIEW CLI the primary report engine and requires explicit path selection to avoid ambient defaults. | The execution-plan builder returns a `LabVIEWCLI.exe` command line with the governed report path, staged VI paths, `-Headless`, and explicit `-LabVIEWPath`. | Unit test |
| VHS-REQ-145 | A ready host-native LVCompare runtime selection shall map to an `LVCompare.exe` command plan with explicit staged VI paths and explicit `-lvpath`. | The authoritative research keeps LVCompare as the fallback engine and requires explicit LabVIEW version selection where possible. | The execution-plan builder returns an `LVCompare.exe` command line using the staged revision files plus explicit `-lvpath`. | Unit test |
| VHS-REQ-146 | The architecture shall treat a `labview2026q1` Windows container provider as the preferred Windows 64-bit report-execution isolation path for extension users when host-native LabVIEW 2026 64-bit session collision is possible, while keeping Windows 32-bit execution host-native. | Extension users may already have LabVIEW 2026 64-bit open on the host, and author direction requires the product to avoid ambient host-session collisions rather than rely on users to close or reconfigure their host setup. | Architecture records separate the Windows 64-bit isolated-container policy from runtime discovery and state that host-native 64-bit execution must not become the assumed default when collision-free isolated execution is available. | ADR and documentation review |
| VHS-REQ-147 | The host-native report-execution runner shall stage the selected left and right revision blobs to the governed staged filenames before launching any NI comparison tool. | The planned command line points at staged revision paths, and execution must not depend on ambient repo working files. | A ready runtime execution writes the selected left and right revision blobs to the governed staged paths before command launch. | Unit test |
| VHS-REQ-148 | The host-native report-execution runner shall execute the governed command plan exactly, retain stdout/stderr artifacts, and record exit code plus duration for the attempted NI tool invocation. | Developers need explicit execution evidence and troubleshooting facts rather than a silent success/fail surface. | A runtime execution attempt writes stdout/stderr artifacts, records the executed command, exit code, duration, and preserves those facts in the retained packet metadata. | Unit test |
| VHS-REQ-149 | The host-native report-execution runner shall fail closed with explicit retained failure reasons when staged blob preparation fails, command launch fails, the NI tool exits nonzero, or no governed report HTML is produced. | Execution failures should be diagnosable without rereading raw logs or guessing what went wrong. | Runtime execution records stable failure reasons such as staged-blob failure, spawn failure, nonzero exit, or missing report output, and the retained packet shows `runtimeExecutionState: failed`. | Unit test |
| VHS-REQ-150 | The retained comparison-report packet shall render a developer-facing execution summary including command, report availability, failure reason, exit code, duration, and stdout/stderr artifact paths. | The developer experience surface should make runtime behavior understandable without requiring direct metadata inspection. | Packet HTML includes an execution-summary section with command, report-exists fact, failure reason, exit code, duration, and artifact paths. | Unit test |
| VHS-REQ-151 | The comparison-report action shall open the retained packet artifact and surface execution-state facts, while only treating the governed `diff-report-*.html` path as present when actual NI output exists. | The extension should not imply that a generated NI report exists merely because a packet artifact was persisted. | The action returns packet-artifact path plus runtime execution state, and integration proof shows the packet artifact always exists while the governed report file is only read when execution retained `reportExists: true`. | Unit and integration tests |
| VHS-REQ-152 | The repo shall provide a canonical comparison-report smoke command for `HARNESS-VHS-001` that runs preflight, runtime selection, retained packet persistence, and attempted NI report execution on the latest comparable retained revision pair. | The active runtime-execution tranche needs a real-history smoke lane that proves the governed report path beyond mocked unit coverage. | `npm run harness:report:smoke` writes retained JSON, Markdown, and HTML smoke artifacts for the canonical harness and records the selected/base pair plus report outcome. | Smoke test |
| VHS-REQ-153 | The canonical comparison-report smoke artifacts shall retain factual report-execution evidence including report status, runtime execution state, provider, engine, blocked/failure reason, generated-report presence, and packet/report/metadata paths. | Developers need a local evidence pack for live report generation attempts without reopening raw packet metadata manually. | The retained smoke report surfaces those runtime facts in JSON, Markdown, and HTML. | Smoke test and unit test |
| VHS-REQ-154 | The canonical comparison-report smoke CLI shall accept explicit runtime-selection overrides for platform, preferred bitness, LabVIEW CLI path, LabVIEW executable path, and LVCompare path. | Live NI report proof on developer machines should not rely on ambient defaults when tool locations or host platform need to be forced explicitly. | The CLI parses those overrides deterministically and forwards them into runtime selection for the smoke run. | Unit test |
| VHS-REQ-155 | The runtime locator shall retain actionable diagnostic notes when report generation is blocked because the LabVIEW runtime or comparison tool is missing. | A blocked-runtime fact should tell the developer what to configure next instead of only naming the missing component. | Missing-runtime/tool selections retain notes pointing to `viHistorySuite.labviewExePath`, `viHistorySuite.labviewCliPath`, or `viHistorySuite.lvComparePath` as appropriate. | Unit test |
| VHS-REQ-156 | The canonical comparison-report smoke artifacts shall retain runtime diagnostic notes alongside blocked or failed report-generation facts. | The live smoke packet should remain actionable without reopening the underlying packet metadata or reading raw logs. | The smoke JSON, Markdown, and HTML outputs include the runtime notes from the selected runtime locator result. | Unit test and smoke test |

## Assumptions

- VS Code desktop with Node extension host is the first target runtime.
- Git is available for CLI-backed history operations.
- Report generation will be added after the local history workflow is stable.

## Constraints

- public repository with restrictive source-available licensing
- sole-author maintenance model
- no external contribution intake by default
