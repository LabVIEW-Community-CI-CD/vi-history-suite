# Software Requirements Specification

## Document Control

- System: `vi-history-suite`
- Version: `v0.1.0-draft`
- Owner: sole author
- Status: draft baseline

## Scope

- Purpose: deliver a governed VS Code extension baseline for content-detected VI
  history review in Git repositories, aligned to the authoritative research
  packet under `docs/research/authoritative/`.
- In scope: content detection, eligibility indexing, authoritative menu gating,
  history panel, report planning, runtime/tool detection, local CI, and
  repo-native governance artifacts.
- Out of scope for the current implemented tranche: live NI report execution,
  TimelineProvider publishable support, VS Code for Web, and external runtime
  dependencies on other VI-history repos.

## Requirements

| ID | Requirement | Rationale | Fit Criterion | Verification |
| --- | --- | --- | --- | --- |
| VHS-REQ-001 | The extension shall detect candidate LabVIEW VIs by reading bytes 8..11 and accepting `LVIN` or `LVCC`. | The product is content-based, not extension-based. | Positive and negative fixtures classify correctly. | Unit test |
| VHS-REQ-002 | The extension shall not rely on filename or file extension for VI detection. | The research paper forbids extension-based gating. | Files without `.vi` are still eligible when bytes match. | Unit and integration tests |
| VHS-REQ-003 | The extension shall support an optional stricter mode that also checks the leading `RSRC` header bytes. | The paper recommends stricter-mode mitigation against false positives. | Strict mode rejects a buffer with only offset-8 magic. | Unit test |
| VHS-REQ-004 | The extension shall contribute a `VI History` context-menu command through both `explorer/context` and `editor/title/context`. | The authoritative research calls for the command to be reachable from Explorer and editor title context menus. | The extension manifest contributes `VI History` through both menu locations. | Unit test and static inspection |
| VHS-REQ-005 | The `VI History` command shall only be visible through the authoritative menu gate `resourcePath in labviewViHistory.eligiblePaths && isWorkspaceTrusted && gitOpenRepositoryCount >= 1`. | The authoritative research defines the precise dynamic visibility gate for the first product surface. | The manifest uses that exact `when` clause for the contributed menu entries. | Unit test and static inspection |
| VHS-REQ-006 | The `VI History` command shall only be visible for tracked files that satisfy VI detection and have at least two modifying commits. | The history panel should only appear when history exists. | Eligibility context excludes files with fewer than two commits. | Unit and integration tests |
| VHS-REQ-007 | The extension shall enumerate tracked files using `git ls-files -z` or a functionally equivalent Git API-backed result. | NUL-safe tracked enumeration is the recommended baseline. | Indexer parses tracked files robustly, including paths with spaces. | Unit test |
| VHS-REQ-008 | The extension shall determine minimum eligibility history with a bounded query equivalent to `git log -n 2 --format=%H --follow -- <path>`. | The product only needs two commits to establish useful history. | Eligibility queries read at most two hashes per file. | Code review and unit test |
| VHS-REQ-009 | The extension shall treat rename-following as best effort and scope it to single-file history queries. | `--follow` is single-file and imperfect. | Documentation and code reflect best-effort rename tracking. | Documentation review |
| VHS-REQ-010 | The extension shall use partial local file reads for `file` URIs during VI detection. | Minimal I/O keeps indexing lightweight. | The local detection path reads only the bytes needed for signature checks. | Unit test and code review |
| VHS-REQ-011 | The extension shall fall back to `workspace.fs.readFile` for non-`file` URI schemes. | Remote and virtual workspaces may not expose local paths. | Detection succeeds or fails cleanly on non-file scheme stubs. | Unit test |
| VHS-REQ-012 | The extension shall gate scanning and external process execution on workspace trust. | Workspace trust is the correct safety boundary for this extension type. | Untrusted workspaces set an empty eligibility context. | Integration test and code review |
| VHS-REQ-013 | The extension shall maintain dynamic menu visibility through `setContext` object membership under `labviewViHistory.eligiblePaths`. | The authoritative research uses the `labviewViHistory.eligiblePaths` membership map as the dynamic menu gate. | The context key `labviewViHistory.eligiblePaths` is populated with eligible resource paths. | Unit test and code review |
| VHS-REQ-014 | The extension shall cache eligibility results by repository root, relative path, and `HEAD`. | Re-indexing every file on every event is wasteful. | Cache keys include repo root, file path, and HEAD. | Code review |
| VHS-REQ-015 | The extension shall bound indexing concurrency through configuration. | Large repositories require controlled background work. | The indexer respects `viHistorySuite.maxIndexedConcurrency`. | Unit test |
| VHS-REQ-016 | The extension shall open a webview history panel for the selected file. | The first review surface is a WebviewPanel. | Command execution opens a panel with file and commit metadata. | Integration test |
| VHS-REQ-017 | The history panel shall show repository name, relative path, VI signature, and commit facts. | The panel is intended for factual developer review. | Rendered HTML includes those data fields. | Unit test |
| VHS-REQ-018 | The history panel shall expose actions to open a revision, diff against the previous revision, and copy a commit hash. | These are the core review actions in the design paper. | Rendered HTML and command handlers support those actions. | Unit and integration tests |
| VHS-REQ-019 | The repo shall remain self-contained and shall not depend on `comparevi-history` or `compare-vi-cli-action`. | The new product should iterate locally without upstream churn. | No package or source dependency references those repos. | Static inspection |
| VHS-REQ-020 | The repo shall define the first real-history harness against a cloned external Git repository rather than vendored history. | Real Git history must stay external to this repo. | `HARNESS-VHS-001` is documented against `ni/labview-icon-editor`. | Documentation review |
| VHS-REQ-021 | The repo shall provide a clone-on-demand canonical harness smoke command for `HARNESS-VHS-001`. | The first live path should be runnable locally without manual repo setup. | `npm run harness:smoke` produces a factual harness report. | Smoke test |
| VHS-REQ-022 | The canonical harness smoke command shall reuse the same core history-model logic as the extension runtime. | The smoke path should validate product logic, not a parallel implementation. | Shared core modules are used by both the extension wrapper and harness smoke runner. | Static inspection and unit test |
| VHS-REQ-023 | The canonical harness smoke command shall write factual JSON, Markdown, and HTML reports under an ignored cache path. | Local review needs retained evidence without polluting tracked source. | Smoke outputs are written under `.cache/harness-reports/`. | Smoke test |
| VHS-REQ-024 | The repo shall include a real-Git unit test for the core history model using a temporary local repository. | Core history behavior should be verified without requiring VS Code runtime. | Tests create a local Git repo, commit a VI fixture twice, and assert eligibility/history. | Unit test |
| VHS-REQ-025 | The repo shall provide a VS Code extension-host integration test runner for the actual `VI History` command flow. | The extension should prove behavior in a real extension host, not only through unit tests. | `npm run test:integration` executes extension-host tests successfully. | Integration test |
| VHS-REQ-026 | The extension-host integration runner shall provision a temporary Git workspace containing both eligible and ineligible content-detected files. | Command gating must be proven against real workspace state. | The integration fixture workspace includes one two-commit VI candidate and one ineligible file, and tests assert different outcomes. | Integration test |
| VHS-REQ-027 | The history panel HTML shall expose stable semantic anchors for status, repository metadata, commit rows, and review-action controls. | Developer-facing review surfaces need deterministic structure for verification and future UX evolution. | Rendered HTML includes stable `data-testid` hooks for the status block, metadata fields, commit rows, and action controls. | Unit and integration tests |
| VHS-REQ-028 | The extension-host integration lane shall verify that the rendered history panel HTML includes factual eligibility, signature, path, and commit-subject content for an eligible file. | Opening a panel is not enough; the surfaced report must be proven to contain the expected review facts. | The real extension-host suite asserts the opened panel HTML contains the eligible file path, signature, eligibility state, and both retained commit subjects. | Integration test |
| VHS-REQ-029 | The extension-host integration lane shall verify `copyHash`, `openCommit`, and `diffPrevious` behavior through the real panel message handler. | Core review actions must be proven on the same action path used by the webview surface. | The real extension-host suite dispatches those three actions through the captured panel handler and asserts clipboard or Git/diff outcomes. | Integration test |
| VHS-REQ-030 | The test API shall retain factual summaries of the last panel action outcome for review-action verification. | Integration evidence should report what action was handled without depending on incidental UI state alone. | The exported extension test API returns the last action summary and action count after dispatched panel actions. | Integration test |
| VHS-REQ-031 | The history panel shall include a chronology packet that states retained revision count plus newest and oldest retained commit summaries. | Developers need immediate chronology cues without parsing the full table first. | Rendered HTML includes anchored chronology fields for order, retained revision count, newest commit summary, and oldest commit summary. | Unit and integration tests |
| VHS-REQ-032 | The history table shall show explicit compare-base context for each retained commit row. | Commit-to-commit review should not require inferring which prior revision a diff action targets. | Each rendered commit row includes a compare-base field showing the previous retained hash or an oldest-revision marker. | Unit and integration tests |
| VHS-REQ-033 | The history panel shall state factual binary-review limitations for Git-backed LabVIEW VI revisions. | The review surface should explain what the panel proves and what still depends on external tooling. | Rendered HTML includes a retained note that VI revisions are binary artifacts and that open/diff actions delegate to VS Code handlers and installed tooling. | Unit and integration tests |
| VHS-REQ-034 | The history table shall retain an explicit selected-versus-base pairing for rows that can diff against a previous retained revision. | Reviewers should not have to infer which revision pair a `Diff prev` action will use. | Rendered HTML includes a pairing element that states the selected retained hash and the retained base hash for diffable rows. | Unit and integration tests |
| VHS-REQ-035 | The history panel shall retain reviewer guidance for using chronology facts, compare pairs, and binary-review actions safely. | The review surface should help developers understand how to use the packet without overclaiming what the panel itself proves. | Rendered HTML includes a guidance block with retained steps covering chronology review, compare-pair interpretation, and binary-review escalation. | Unit and integration tests |
| VHS-REQ-036 | The history panel shall retain a confidence-and-scope packet with stable anchors. | Reviewers need an explicit place to judge what the current surface proves and where its boundaries are. | Rendered HTML includes a retained confidence-and-scope block with stable verification hooks. | Unit and integration tests |
| VHS-REQ-037 | The confidence-and-scope packet shall state the direct local evidence basis and the review scope included in this surface. | Confidence claims should be grounded in concrete evidence sources, not vague sentiment. | Rendered HTML states that the packet is based on local Git history, tracked-file status, and content-detected VI signature checks, and that it directly covers chronology, path provenance, retained hashes, and command routing. | Unit and integration tests |
| VHS-REQ-038 | The confidence-and-scope packet shall state which review questions remain outside scope without external binary comparison tooling. | The panel should fail closed on binary semantic and cosmetic claims it cannot prove by itself. | Rendered HTML states that binary semantic differences, visual/cosmetic change detection, and NI comparison-report output require external comparison tooling. | Unit and integration tests |
| VHS-REQ-039 | The history panel shall expose a panel-level `Copy review packet` action. | Reviewers need a portable artifact they can paste into human review flows without recreating the packet manually. | Rendered HTML includes a panel-level copy action that dispatches without requiring a commit hash. | Unit and integration tests |
| VHS-REQ-040 | The `Copy review packet` action shall copy a factual plain-text packet grounded in the same local review evidence shown in the panel. | The portable artifact should preserve the panel’s governed facts rather than invent a separate summary path. | The copied text includes repository, path, signature, retained revision count, confidence/scope statements, and retained compare pairs from the current review packet. | Unit and integration tests |
| VHS-REQ-041 | The repo shall provide a single-command local design gate for the governed development baseline. | The continuous design gate should self-propel development instead of relying on remembered manual command sequences. | `npm run design:gate` executes successfully and writes retained design-gate reports under `.cache/design-gate/`. | Unit test and gate run |
| VHS-REQ-042 | The local design gate shall run unit coverage, extension-host integration, canonical harness smoke, and standards assurance sequentially. | The repo should fail closed unless both product behavior and governed assurance pass in one lane. | The design-gate plan includes those four ordered steps and the live gate run records them. | Unit test and gate run |
| VHS-REQ-043 | The local design gate shall retain machine-readable and Markdown reports for the latest run. | A self-propelling gate needs durable local evidence for later review, not just terminal output. | The gate writes `latest-report.json` and `latest-report.md` under `.cache/design-gate/`, including step status and retained assurance summary when available. | Unit test and gate run |
| VHS-REQ-044 | The local design gate shall retain the weakest covered source files from the latest unit-coverage summary. | A self-propelling gate should derive the next development focus from retained evidence, not operator memory. | The retained gate report records weakest source files ordered deterministically by line percentage, then larger covered-total scope, then relative path. | Unit test and gate run |
| VHS-REQ-045 | The local design gate shall surface a retained next-focus cue derived from the weakest retained source-coverage fact. | The autonomous lane should identify the next improvement target deterministically. | The retained gate report and Markdown summary state the weakest source file as the next focus. | Unit test and gate run |
| VHS-REQ-046 | The local design gate shall state an explicit reason when coverage-focus facts are unavailable. | Missing coverage evidence should fail closed in the report instead of disappearing silently. | When the coverage summary cannot be read or yields no source entries, the retained report states a machine-readable unavailable reason. | Unit test |
| VHS-REQ-047 | The local design gate orchestration shall live in a reusable shared runner that is invoked by the CLI entrypoint. | Self-propelling development should be testable below the CLI shell boundary. | The CLI entrypoint delegates to a shared runner module that can be unit-tested without spawning the real gate. | Static inspection and unit test |
| VHS-REQ-048 | The local design gate shall retain a fail report when a gated step stops the sequence before completion. | Autonomous development needs durable failure evidence, not only successful-run records. | When a step fails, the retained JSON and Markdown reports still record the failing status, completed steps, and derived next-focus data when available. | Unit test |
| VHS-REQ-049 | The `VI History` command shall show an informational message and stop cleanly when invoked without a selected resource and without an active editor target. | Silent no-ops weaken developer experience and obscure why no review packet appeared. | Invoking the command with no URI target and no active editor shows a user-facing informational message and does not create a panel. | Unit test |
| VHS-REQ-050 | The `VI History` command shall show a warning and stop without loading history when the workspace is untrusted. | Workspace-trust gating should be explicit to the developer, not only implicit in missing behavior. | In an untrusted workspace, the command shows a warning and does not call the history loader or open a panel. | Unit test |
| VHS-REQ-051 | The `VI History` command shall show an informational message and stop without loading history when the selected file is not currently eligible. | Ineligible-file handling should explain why the review surface was not opened. | For an ineligible file, the command shows an informational message and does not call the history loader or open a panel. | Unit test |
| VHS-REQ-052 | History-panel actions that require Git-backed revision URIs shall show a warning and retain a `missing-git-uri` action outcome when the Git URI cannot be resolved. | Review-action failures should be actionable for the developer and explicit in retained evidence. | `openCommit` and `diffPrevious` retain `missing-git-uri` and show a warning when Git-backed revision URIs are unavailable. | Unit test |
| VHS-REQ-053 | The `diffPrevious` panel action shall show an informational message and retain a `missing-previous-hash` outcome when no retained base revision exists. | Reviewers need explicit guidance when a retained row cannot be diffed against a previous revision. | Dispatching `diffPrevious` for a commit without `previousHash` shows an informational message, records `missing-previous-hash`, and does not execute a diff command. | Unit test |
| VHS-REQ-054 | History-panel action handling shall ignore unsupported or hashless action messages without executing Git, open, or diff side effects, while retaining an explicit tracker outcome. | The command layer should fail closed on malformed or unsupported panel traffic. | Hashless messages retain `ignored-missing-hash`, unsupported messages retain `unsupported-command`, and neither path executes Git/open/diff side effects. | Unit test |
| VHS-REQ-055 | The harness smoke CLI shall default to `HARNESS-VHS-001` when no harness identifier is supplied. | The canonical live harness should remain the deterministic default entrypoint. | Running the CLI with no arguments resolves `HARNESS-VHS-001` as the selected harness. | Unit test |
| VHS-REQ-056 | The harness smoke CLI shall accept `--harness-id` and `--strict-rsrc-header` as explicit governed options. | Local harness iteration needs a small stable option surface for selecting a harness and detection strictness. | Parsing the CLI arguments retains the selected harness id and strict-header flag in the harness-run request. | Unit test |
| VHS-REQ-057 | The harness smoke CLI shall print deterministic success lines for retained JSON, Markdown, HTML, eligibility, signature, and commit-count facts. | Developers need a stable command-line summary that aligns with the retained report artifacts. | A successful CLI run writes those summary lines in a deterministic order. | Unit test |
| VHS-REQ-058 | The harness smoke CLI shall expose a reusable runner below the shell boundary. | The harness entrypoint should be directly testable without spawning a separate Node process. | The CLI shell entrypoint delegates to an exported reusable runner function. | Static inspection and unit test |
| VHS-REQ-059 | The harness smoke CLI shall reject unknown arguments or a missing `--harness-id` value with usage guidance. | Silent acceptance of malformed CLI input is a weak design for a governed harness surface. | Invalid arguments throw a usage-bearing error before the harness run begins. | Unit test |
| VHS-REQ-060 | The harness smoke CLI shall support `--help` and exit cleanly without running the harness. | Operators need a deterministic way to discover the governed option surface without side effects. | `--help` returns a help status, prints usage text, and does not invoke the harness runner. | Unit test |
| VHS-REQ-061 | The VI history service shall select the most specific matching Git repository root when multiple Git API repositories prefix the target path. | Overlapping repository roots are a real weak-design seam for nested repositories. | Repository-root resolution chooses the longest matching Git API root for the target path. | Unit test |
| VHS-REQ-062 | The VI history service shall fall back to Git CLI repository-root resolution when no Git API repository matches the target path. | The service must remain functional when the Git extension has no matching repository context. | If no Git API match exists, the service resolves the repository root via `git rev-parse --show-toplevel` on the file’s parent directory. | Unit test |
| VHS-REQ-063 | The VI history service shall forward `strictRsrcHeader` and `maxHistoryEntries` settings to the shared history-model loader. | Service-level configuration should deterministically shape the loaded review packet. | The load path passes the current configuration values into the shared history-model loader options. | Unit test |
| VHS-REQ-064 | The VI history service shall expose Git-URI translation only through the available Git API. | The service should not synthesize Git revision URIs outside the Git extension contract. | `toGitUri` delegates to the Git API when present and returns `undefined` otherwise. | Unit test |
| VHS-REQ-065 | The URI-based VI magic wrapper shall delegate `file` URIs to the file-system probe helper. | Local file probes should continue to use the bounded file-read path instead of the workspace-fs fallback. | `readViProbeBytes` calls the file-system probe helper for `file` URIs and does not call `workspace.fs.readFile` in that path. | Unit test |
| VHS-REQ-066 | The URI-based VI magic wrapper shall truncate non-`file` workspace-fs reads to the minimum detection-header length. | Remote and virtual workspace reads should retain only the bytes required for signature detection. | `readViProbeBytes` returns only the first detection-header bytes from `workspace.fs.readFile` for non-`file` URIs. | Unit test |
| VHS-REQ-067 | The URI-based VI magic wrapper shall fail closed when probe reads throw. | Detection wrappers should return `undefined` or `false` rather than surfacing probe exceptions as product behavior. | `detectViSignatureFromUri` returns `undefined` and `isLabviewViByMagic` returns `false` when probe reads fail. | Unit test |
| VHS-REQ-068 | The built-in Git API resolver shall return `undefined` when the `vscode.git` extension is not installed. | The extension should degrade gracefully when the built-in Git extension is unavailable. | `getBuiltInGitApi()` returns `undefined` without attempting activation when `vscode.extensions.getExtension('vscode.git')` returns no extension. | Unit test |
| VHS-REQ-069 | The built-in Git API resolver shall request API version `1` from the active or activated `vscode.git` extension. | The repo should use a stable, governed API version rather than an implicit extension contract. | `getBuiltInGitApi()` returns the value from `getAPI(1)` and activates the Git extension only when it is not already active. | Unit test |
| VHS-REQ-070 | The built-in Git API resolver shall return `undefined` when the Git extension exports do not provide `getAPI`. | The product should fail closed rather than assuming an incompatible Git extension export surface. | `getBuiltInGitApi()` returns `undefined` when the active or activated extension exports lack a callable `getAPI`. | Unit test |
| VHS-REQ-071 | The built-in Git API resolver shall fail closed when Git extension activation throws. | Git extension activation failure should not crash the product’s local review path. | `getBuiltInGitApi()` returns `undefined` when `extension.activate()` throws. | Unit test |
| VHS-REQ-072 | The eligibility indexer shall clear retained eligible-path state and indexed repository roots when the workspace is untrusted. | Trust gating should remove stale review visibility instead of leaving previously indexed state behind. | An untrusted refresh results in empty eligible-path context and an empty indexed-root snapshot. | Unit test |
| VHS-REQ-073 | The eligibility indexer shall register repository state listeners for repositories present at startup and for repositories opened after startup, and dispose those listeners when repositories close. | Repository change tracking should remain correct as the Git repository set changes over time. | The indexer attaches state listeners to existing and newly opened repositories, and disposes the matching listener on repository close. | Unit test |
| VHS-REQ-074 | The eligibility indexer shall fail closed on repository-level Git enumeration errors without aborting the full refresh. | One broken repository should not prevent eligibility updates for other repositories. | If tracked-file or HEAD queries fail for one repository, the refresh still completes and indexes other repositories. | Unit test |
| VHS-REQ-075 | The eligibility indexer shall fail closed on per-file eligibility evaluation errors without aborting the full repository refresh. | One unreadable or malformed file should not prevent eligibility updates for the rest of the repository. | If eligibility evaluation throws for one tracked file, the refresh still completes and only successful eligible files appear in the retained context. | Unit test |
| VHS-REQ-076 | The eligibility indexer shall reuse cached eligibility results for the same repository root, normalized relative path, and HEAD. | Repeated refreshes on the same Git state should avoid re-evaluating unchanged files. | Two refreshes with the same repository root, relative path, and HEAD evaluate a file only once. | Unit test |
| VHS-REQ-077 | The eligibility indexer shall invalidate cached eligibility results when the repository HEAD changes. | Eligibility should be recomputed when the underlying Git state changes. | A refresh with a different HEAD re-evaluates tracked files even if the relative path is the same. | Unit test |
| VHS-REQ-078 | The eligibility indexer shall expose normalized URI path variants in its dynamic context-key set. | Dynamic menu visibility should match equivalent path spellings used by VS Code and Git-backed URIs. | The context-key helper includes original, normalized, and slash-normalized path variants for a URI. | Unit test |
| VHS-REQ-079 | The design-gate runner shall create the retained design-gate report directory recursively before writing latest JSON and Markdown report artifacts. | A self-propelling gate should not rely on a precreated cache directory to retain its evidence. | Persisting a design-gate report first creates `.cache/design-gate/` recursively, then writes `latest-report.json` and `latest-report.md`. | Unit test |
| VHS-REQ-080 | The design-gate runner shall retain spawned-step stdout, stderr, and duration while streaming the same bytes to the active operator output channels. | Autonomous gate debugging needs retained evidence and live operator visibility from the same execution facts. | The shared spawned-step executor returns accumulated stdout, stderr, and duration while forwarding emitted bytes to the configured stdout/stderr sinks. | Unit test |
| VHS-REQ-081 | The design-gate runner shall fail closed when a spawned gate step emits a process error or closes without an exit code. | The autonomous lane should not treat startup failures or ambiguous process termination as a successful gate step. | Spawn errors resolve to exit code `1` with retained error text, and `close(null)` is coerced to exit code `1`. | Unit test |
| VHS-REQ-082 | The extension shall register its primary command under the authoritative command identifier `labviewViHistory.open`. | The authoritative research uses `labviewViHistory.open` as the product command identity for menu wiring and activation. | The manifest and activation events declare `labviewViHistory.open` and the extension registers that command at runtime. | Unit test and static inspection |
| VHS-REQ-083 | The extension manifest shall declare `vscode.git` as an extension dependency and activate through `onStartupFinished` plus `onCommand:labviewViHistory.open`. | The authoritative research directs the extension to depend on the built-in Git extension and avoid startup-heavy eager activation. | The manifest includes `extensionDependencies: ["vscode.git"]`, `onStartupFinished`, and `onCommand:labviewViHistory.open`. | Unit test and static inspection |
| VHS-REQ-084 | The extension manifest shall declare limited untrusted-workspace support and restrict external LabVIEW tool configuration in untrusted workspaces. | The authoritative research directs the product to use VS Code workspace-trust declarations in addition to runtime trust checks. | The manifest declares `capabilities.untrustedWorkspaces.supported = limited` with a description and restricted external-tool configuration keys. | Unit test and static inspection |
| VHS-REQ-085 | The published review surface shall remain WebviewPanel-based, and TimelineProvider shall remain outside the published extension path while it is a proposed API. | The authoritative research explicitly treats TimelineProvider as experimental and non-publishable. | Architecture records state that the published extension uses webview-based review surfaces and does not depend on TimelineProvider. | ADR review |
| VHS-REQ-086 | The indexing experience shall report percent, processed items, and ETA through `window.withProgress`. | The authoritative research specifies a richer progress surface than the current message-only progress. | Indexing progress messages include percent, processed/total, and ETA estimates in the notification progress lane. | Integration test |
| VHS-REQ-087 | The extension shall surface a discreet status-bar progress item for background indexing. | The authoritative research calls for a secondary progress surface that stays visible without blocking the user. | A status-bar item appears during indexing with spinner, percent, processed/total, and ETA, and hides when indexing completes. | Integration test |
| VHS-REQ-088 | The webview review surface shall expose progress updates for long-running report-generation operations. | The authoritative research calls for webview-visible progress feedback during report generation. | The webview handles report-generation progress messages and renders a progress strip or equivalent anchored surface. | Integration test |
| VHS-REQ-089 | Report generation shall verify that both selected revision blobs are LabVIEW VIs by content before invoking external comparison tooling. | The authoritative research requires blob verification as a precondition to compare/report execution. | The report-generation path rejects non-VI revision blobs before tool invocation and retains the failure reason. | Unit and integration tests |
| VHS-REQ-090 | Generated HTML comparison reports shall use the filename pattern `{type}-report-{fullFilename}.html`. | The authoritative research defines the report artifact naming contract explicitly. | Generated HTML report paths preserve the full filename and the requested `{type}-report-` prefix. | Unit and integration tests |
| VHS-REQ-091 | Report generation shall extract same-name revisions to distinct temporary filenames before invoking LVCompare-compatible tooling. | The authoritative research identifies same-name revision comparison as a high-risk LVCompare limitation. | Temporary extracted base/head files use distinct filenames and do not collide when both revisions share the same original filename. | Unit and integration tests |
| VHS-REQ-092 | Report artifacts and report metadata shall be stored under workspace-scoped `context.storageUri` directories. | The authoritative research directs the extension to use workspace storage for generated reports and related metadata. | The report-generation path stores extracted revisions, HTML output, and a small metadata record under `context.storageUri`. | Integration test |
| VHS-REQ-093 | The review surface shall show open-report links only when the generated HTML exists, and shall expose report resources through `asWebviewUri` plus narrow `localResourceRoots`. | The authoritative research requires existence-based report visibility and secure webview resource exposure. | Report links are absent when HTML does not exist and present only through webview-safe URIs rooted in explicitly allowed local resource paths. | Unit and integration tests |
| VHS-REQ-094 | Runtime tool settings shall allow explicit user override through `labviewCliPath`, `lvComparePath`, `labviewExePath`, and `preferBitness`, with user configuration taking precedence over discovery and failing closed when an explicit configured path is missing. | The authoritative research prioritizes explicit user configuration ahead of heuristic tool discovery, and runtime configuration should not silently drift to a different tool when a configured path is invalid. | Settings exist for those tool surfaces, configured paths are checked before auto-detection, and an invalid configured path yields a stable blocked reason. | Unit tests |
| VHS-REQ-095 | Windows runtime discovery shall probe registry views and documented installation roots to locate LabVIEW 2026 Q1 executables and comparison tooling. | The authoritative research defines Windows as the richest supported discovery surface and names registry/install-root probing as the preferred strategy. | Discovery logic retains Windows registry probe plans, parses registry-discovered LabVIEW executables, scans documented install roots, and returns classified host-native candidates. | Unit tests |
| VHS-REQ-096 | macOS and Linux runtime discovery shall scan documented install roots and retain explicit platform availability constraints. | The authoritative research requires cross-platform notes and explicit handling of constrained NI platform support. | Discovery logic scans documented Linux roots, returns a stable unsupported constraint for macOS 2026 Q1, and retains explicit blocked reasons when tooling is unavailable. | Unit tests and documentation review |
| VHS-REQ-097 | The extension shall remain desktop/remote-host oriented and shall not target a publishable VS Code web surface while child-process Git and LabVIEW tooling remain required. | The authoritative research explicitly treats web support as out of target scope for this product shape. | Product and architecture records state that the published extension targets desktop or remote extension hosts only. | ADR and documentation review |
| VHS-REQ-098 | Packaging guidance shall support `vsce package`, local VSIX installation, and documented Marketplace publish prerequisites. | The authoritative research includes a governed packaging and release path for the extension. | Repo documentation defines package, local install, and publish prerequisites for VS Code Marketplace delivery. | Documentation review |
| VHS-REQ-099 | The repo shall provide a pure comparison-report planning module before live LabVIEW report execution is introduced. | The authoritative second-round research recommends isolated report-generation modules so report naming, storage, and engine selection can be governed independently of runtime execution. | A pure module derives report artifact layout and command plans without invoking external tools. | Unit test and ADR review |
| VHS-REQ-100 | The comparison-report planning module shall derive the exact report filename pattern `{type}-report-{fullFilename}.html`. | The authoritative research defines the report artifact naming contract explicitly. | The planning module returns exact filenames such as `diff-report-foo.vi.html` and `print-report-foo.vi.html`. | Unit test |
| VHS-REQ-101 | The comparison-report planning module shall derive distinct staged filenames for left and right revisions of the same VI basename. | The authoritative research identifies same-name revision handling as a hard comparison constraint. | The planning module generates different staged left/right filenames even when both revisions share the same original basename. | Unit test |
| VHS-REQ-102 | The comparison-report planning module shall derive deterministic workspace-storage paths under `context.storageUri/reports/<repoId>/<fileId>/...` plus narrow local-root candidates for later webview exposure. | The authoritative research directs report artifacts to workspace storage and recommends narrow local resource roots. | The planning module derives stable report/staging/metadata paths plus allowed local-root paths from a storage root, repository root, and repo-relative file path. | Unit test |
| VHS-REQ-103 | The primary comparison-report command plan shall target `LabVIEWCLI -OperationName CreateComparisonReport` with asset-backed `HTML` as the default report format. | The authoritative research names `CreateComparisonReport` as the primary programmatic engine, and the live NI proof on LabVIEW 2026 Q1 Windows produces report HTML plus a sibling `_files` asset directory. | The planning module returns an exact primary command plan using `CreateComparisonReport`, `-reportType HTML`, and the governed report path. | Unit test |
| VHS-REQ-104 | The fallback comparison-report command plan shall target `LVCompare` with an optional explicit `-lvpath`. | The authoritative research treats `LVCompare` as the interactive fallback and documents `-lvpath` for explicit LabVIEW selection. | The planning module returns the LVCompare executable plus left/right VI paths and appends `-lvpath` only when an explicit LabVIEW path is supplied. | Unit test |
| VHS-REQ-105 | The core history model shall auto-discover the Git repository root from the target filesystem path when `repoRoot` is not supplied. | The shared history path should work from file selection alone and not require callers to pre-resolve the repository root. | Loading eligibility or history from a tracked VI path without `repoRoot` derives the correct repository root and repo-relative path. | Unit test |
| VHS-REQ-106 | The core history model shall preserve the default non-strict VI signature mode and default history limit when optional overrides are omitted. | The shared model should expose the product's default behavior without forcing callers to restate default options. | A tracked file with offset-8 VI magic but no strict `RSRC` header is accepted in default mode, and loading history without `historyLimit` returns the available commit chain with `previousHash` only on rows that have a predecessor. | Unit test |
| VHS-REQ-107 | The design-gate runner shall support default filesystem-backed coverage reads and report persistence when no injected I/O helpers are supplied. | The continuous gate should work as a real repo-local tool, not only through injected test doubles. | A runner call against a temporary repo writes retained reports under `.cache/design-gate/` and reads coverage focus from an on-disk `coverage/coverage-summary.json` without injected file helpers. | Unit test |
| VHS-REQ-108 | The spawned-step executor shall retain the first terminal result and ignore later child-process `error` or `close` events after settlement. | The gate should fail deterministically even when child-process event ordering is noisy. | After a spawned step settles, later `error` or `close` events do not alter the retained exit code, stdout, stderr, or duration. | Unit test |
| VHS-REQ-109 | The canonical harness registry shall fail closed for unknown harness identifiers with a stable error message. | The governed harness layer should not silently invent or coerce unsupported harness IDs. | Resolving an unknown harness ID throws `Unknown harness id: <id>`. | Unit test |
| VHS-REQ-110 | The design-gate CLI script entrypoint shall route the retained `runDesignGateCliMain` exit code into `process.exitCode` without forcing an immediate process exit. | The CLI should remain script-friendly and testable while preserving deterministic shell status. | A script-mode helper applies a supplied exit code to a writable `process`-like target, and the top-level module branch uses that helper. | Unit test and static inspection |
| VHS-REQ-111 | The harness-smoke CLI script entrypoint shall route retained success or failure into `process.exitCode` and a stable stderr message without forcing an immediate process exit. | The governed harness CLI should match the deterministic, script-friendly behavior of the design-gate CLI. | A script-mode helper returns `false` when not running as main, returns `true` when active, writes a stable error line on failure, and applies the resulting exit code to a writable `process`-like target. | Unit test and static inspection |
| VHS-REQ-112 | The Git CLI helper layer shall return trimmed repository HEAD, repository root, and tracked-file facts from a real temporary Git repository. | The wrapper functions around `git rev-parse` and `git ls-files -z` should be proven against real Git behavior, not only parser stubs. | A unit test initializes a temporary Git repo, commits tracked files, and verifies `getRepoHead`, `getRepoRoot`, and `listTrackedFiles` against the real repo state. | Unit test |
| VHS-REQ-113 | The Git CLI helper layer shall return bounded commit hashes and structured history entries for a tracked file from a real temporary Git repository. | The bounded-history and structured-history wrappers should be proven against real Git behavior, not only via higher-level consumers. | A unit test commits a tracked file multiple times, then verifies `getFileCommitHashes` respects the requested limit and `getFileHistoryEntries` returns ordered history records with hash, author date, author name, and subject. | Unit test |
| VHS-REQ-114 | The Git CLI helper layer shall fail closed on Git subprocess errors and shall fall back to the bare `git` executable when no override or Windows candidate path is available. | The Git runtime surface should remain deterministic both when invocation fails and when no explicit Windows installation path can be resolved. | A unit test proves `resolveGitExecutable` returns `git` when no override/candidate exists, and `runGit` rejects when Git returns a subprocess error. | Unit test |
| VHS-REQ-115 | The filesystem VI-probe helper shall return only the bytes actually read, up to the minimum detection-header length. | The file-backed probe path should remain bounded and should not fabricate bytes beyond the real file contents. | Reading a short file returns exactly the bytes present, and reading a longer file returns only the minimum header-length probe window. | Unit test |
| VHS-REQ-116 | The filesystem VI-signature detector shall fail closed to `undefined` when the probe read cannot be completed. | The direct filesystem detection layer should not leak file I/O failures as uncaught exceptions. | Probing a missing or unreadable file resolves to `undefined` instead of throwing. | Unit test |
| VHS-REQ-117 | The eligibility indexer shall debounce workspace-folder and workspace-trust refresh triggers, and it shall tolerate startup with no Git API. | The indexer should avoid redundant refresh storms and should still function in CLI-only or Git-extension-unavailable environments. | Starting without a Git API still registers workspace listeners, and multiple rapid listener triggers schedule only one additional refresh after the debounce window. | Unit test |
| VHS-REQ-118 | The eligibility indexer shall ignore duplicate repository listener registrations and repositories that do not expose a state-change emitter. | Repository listener management should fail closed instead of stacking duplicate listeners or assuming incomplete Git API surfaces. | Opening the same repository twice or a repository without `state.onDidChange` does not create duplicate listener registrations. | Unit test |
| VHS-REQ-119 | The eligibility indexer context-key helper shall ignore empty values and add lowercase path variants on Windows. | Dynamic menu gating should be robust across empty inputs and Windows path-case behavior. | Empty `fsPath`/`path` values yield no context keys, and Windows mode includes lowercase filesystem and slash-normalized variants. | Unit test |
| VHS-REQ-120 | The eligibility indexer concurrent worker loop shall stop cleanly when workers outnumber remaining queue items. | Queue exhaustion should not create undefined-worker side effects or hangs under higher configured concurrency. | `forEachConcurrent` processes each value once and resolves cleanly when more workers exist than queued items. | Unit test |
| VHS-REQ-121 | The comparison-report staging planner shall fall back to deterministic `left-` and `right-` filename prefixes when revision identifiers are omitted or blank. | The report subsystem still needs stable staged filenames before revision hashes are available. | A staged revision plan without usable revision identifiers produces `left-<filename>` and `right-<filename>` paths. | Unit test |
| VHS-REQ-122 | The comparison-report planning subsystem shall fail closed on empty required identifiers and paths instead of producing malformed artifact or command plans. | Empty planning inputs are a weak design for a governed report-generation contract. | Public planning helpers throw stable non-empty-field errors when required storage roots, filenames, or VI/report paths are blank. | Unit test |
| VHS-REQ-123 | The history panel and copied review packet shall render an explicit `No retained commits` fallback when the current model has no retained commits. | The review surface should stay truthful and readable even when history facts cannot currently populate newest/oldest summaries. | HTML and plain-text packet output render `No retained commits` for newest/oldest summaries when `commits` is empty. | Unit test |
| VHS-REQ-124 | The design-gate runner shall retain the coverage-focus unavailable reason and omit the retained `nextFocus` cue when no governed source coverage facts are available. | The autonomous gate should not invent a next tranche when the retained source-coverage evidence is unavailable. | A retained gate report with an unavailable coverage-focus reason leaves `nextFocus` undefined and preserves the unavailable reason in JSON and Markdown outputs. | Unit test |
| VHS-REQ-125 | The spawned design-gate step runner shall derive step duration from the default wall clock when no injected time source is provided. | The governed step runner should remain deterministic in production use even when tests are not injecting a synthetic clock. | A spawned step using the default clock path records duration from `Date.now()` and retains streamed stdout/stderr facts. | Unit test |
| VHS-REQ-126 | The harness-smoke runner shall stamp generated reports from the default ISO timestamp path when no injected clock is provided. | The retained harness report should stay deterministic in production use even when tests are not injecting a synthetic time source. | A harness-smoke run without a supplied `now` dependency records `generatedAt` from the default `new Date().toISOString()` path and persists the same timestamp into JSON output. | Unit test |
| VHS-REQ-127 | The comparison-report subsystem shall derive revision-blob Git specifiers as `<revision>:<normalized/relative/path>` before report preflight or staging. | Report-generation preflight should use a deterministic Git object selector instead of ad hoc path assembly. | A normalized relative path such as `Tooling\deployment\foo.vi` yields a blob specifier like `abcdef123456:Tooling/deployment/foo.vi`. | Unit test |
| VHS-REQ-128 | The comparison-report subsystem shall verify both selected revision blobs as content-detected VIs before compare/report generation is considered ready. | The authoritative research requires both revisions to be proven VI blobs before any LVCompare or LabVIEWCLI report flow proceeds. | A preflight run against two Git revisions reports `ready: true` only when both selected blobs resolve to `LVIN` or `LVCC` by content detection. | Unit test |
| VHS-REQ-129 | The comparison-report subsystem shall fail closed with explicit side-specific blocked reasons when a selected revision blob cannot be read or is not a VI. | Report generation should not proceed on ambiguous or invalid revision inputs. | A preflight run reports `left-blob-not-vi`, `left-blob-read-failed`, `right-blob-not-vi`, or `right-blob-read-failed` when either selected blob is invalid or unreadable. | Unit test |
| VHS-REQ-130 | The local design gate shall suppress the coverage-based `nextFocus` cue when all governed source files have reached 100% line coverage. | A 100%-covered tie should not masquerade as a real next file-level improvement target. | The retained gate report omits `nextFocus` when the weakest retained source entry is already at 100.0% line coverage. | Unit test and gate run |
| VHS-REQ-131 | The local design gate shall derive the next product tranche from a governed development queue when line coverage is saturated. | The self-propelling gate should continue driving implementation after the line-coverage baseline is complete. | When the weakest retained source coverage fact is 100.0%, the retained gate report records the first active or queued tranche from `docs/product/development-queue.json`. | Unit test and gate run |
| VHS-REQ-132 | The local design gate shall retain an explicit unavailable reason when the governed development queue cannot yield an active or queued tranche. | Missing product-queue guidance should fail closed instead of silently dropping the next tranche signal. | Reading an empty or unavailable development queue records a stable unavailable reason in the retained report. | Unit test |
| VHS-REQ-133 | The VI History panel shall expose the retained-pair report actions only for retained revisions that have a retained base revision, using `Generate compare` when retained pair evidence is absent, `Refresh compare` when retained pair evidence is present, and `Open compare` only when retained pair evidence is present, while keeping the unavailable actions disabled for rows that cannot support them. | Pairwise VI review needs a clear distinction between opening retained comparison evidence and creating or refreshing that evidence, while still preventing base-less oldest revisions from implying a compare operation that cannot exist. | Rows with `previousHash` and no retained pair evidence render disabled `Open compare` plus enabled `Generate compare`; rows with retained pair evidence render enabled `Open compare` plus enabled `Refresh compare`; rows without `previousHash` render the unavailable actions disabled. | Unit and integration test |
| VHS-REQ-134 | The comparison-report action shall fail closed when `context.storageUri` is unavailable and shall inform the user that workspace-scoped extension storage is required. | The authoritative storage contract depends on workspace-scoped storage and should not silently fall back to temp or repo paths. | Triggering `Generate report` without workspace storage yields a stable warning and records the missing-storage outcome. | Unit test |
| VHS-REQ-135 | The comparison-report action shall persist a retained packet artifact and metadata under `context.storageUri/reports/<repoId>/<fileId>/`, while reserving `{type}-report-{fullFilename}.html` as the governed NI-generated report output path. | The authoritative report filename must remain available for actual NI output instead of being consumed by a placeholder packet surface. | Triggering `Generate report` writes a retained packet artifact, `report-metadata.json`, and staged left/right filenames under the workspace-scoped reports root, while the governed `diff-report-<fullFilename>.html` path remains the runtime output target. | Unit and integration test |
| VHS-REQ-136 | The comparison-report action shall surface the retained packet artifact through a dedicated webview panel using `asWebviewUri` and `localResourceRoots` limited to the workspace storage root and repo-specific report root. | Report artifacts should be surfaced through a least-privilege webview path consistent with the authoritative research. | The comparison-report panel opens with `localResourceRoots` restricted to workspace storage and the repo-specific report root and loads the retained packet artifact through an `asWebviewUri` iframe. | Unit test |
| VHS-REQ-137 | The retained comparison-report packet shall state truthful runtime-execution status and retain preflight, runtime-selection, and execution facts whether or not actual NI HTML report generation succeeds. | The stored packet must remain truthful after live NI runtime execution is introduced. | Stored packet HTML and metadata record `runtimeExecutionState`, readiness facts, execution failure reasons when present, and whether the governed report output currently exists. | Unit and integration test |
| VHS-REQ-138 | The report-generation architecture shall preserve a runtime-provider boundary for Windows 64-bit execution so an isolated LabVIEW 2026 Q1 Windows container path can be introduced without colliding with an already-open host-native 64-bit LabVIEW session. | Author direction requires the future 64-bit runtime path to avoid host-session collisions while keeping the current repo self-contained and explicit about provider choice. | Architecture records state that Windows 64-bit report execution remains behind a provider boundary, with host-native selection implemented now and isolated container execution reserved as the preferred future isolation path. | ADR and documentation review |
| VHS-REQ-139 | The comparison-report action shall resolve the governed runtime/tool selection before persisting a report packet and shall retain that runtime summary with the selected revision pair. | Runtime detection should inform the stored report surface now, even before live NI execution is wired. | Triggering `Generate report` records the selected provider, engine, and runtime blocked reason or notes in the stored packet metadata. | Unit and integration tests |
| VHS-REQ-140 | The retained comparison-report packet shall distinguish `blocked-runtime` from `blocked-preflight` and shall record runtime-selection facts when NI report execution cannot currently proceed. | Preflight validity and runtime availability are separate truths and should not be collapsed into one blocked state. | A packet with valid VI blobs but unavailable tooling records `reportStatus: blocked-runtime`, `runtimeExecutionState: not-available`, and the runtime selection summary. | Unit and integration tests |
| VHS-REQ-141 | The stored comparison-report HTML shall render the runtime provider, engine, blocked reason, selected tool paths, and retained runtime notes. | The HTML packet is part of the product surface and should expose runtime readiness facts without requiring metadata inspection. | Rendered report HTML includes a runtime-selection section with provider, engine, blocked reason, tool paths, and runtime notes. | Unit test |
| VHS-REQ-142 | The repo shall provide a pure execution-plan builder that maps a ready retained report packet to the exact LabVIEW CLI or LVCompare command invocation without executing external tooling. | The active runtime-execution tranche needs a governed bridge from retained packet facts to concrete tool invocation, while keeping the first slice static and testable. | A pure module accepts a retained packet and returns either a blocked reason or a fully specified executable-plus-args command plan. | Unit test |
| VHS-REQ-143 | The execution-plan builder shall fail closed when the retained packet is `blocked-preflight`, `blocked-runtime`, or lacks the tool-specific fields required by the selected runtime engine. | Runtime execution should not start from ambiguous or incomplete packet state. | The execution-plan builder returns stable blocked reasons for preflight/runtime blocks and incomplete CLI/LVCompare selections. | Unit test |
| VHS-REQ-144 | A ready LabVIEW CLI runtime selection shall map to a `CreateComparisonReport` command plan with asset-backed `HTML`, explicit staged VI paths, explicit output path, and explicit LabVIEW selection fields, while runtime-startup mitigation remains owned by the runtime executor. | The authoritative research makes LabVIEW CLI the primary report engine, and the live NI proof requires the executor to own startup mitigation such as headless prelaunch, INI timeout tuning, and retry behavior instead of collapsing those behaviors into the static plan. | The execution-plan builder returns a `LabVIEWCLI.exe` command line with the governed report path, staged VI paths, `-reportType HTML`, and explicit LabVIEW selection fields that the runtime executor can run on host-native or containerized Windows paths. | Unit test |
| VHS-REQ-145 | A ready host-native LVCompare runtime selection shall map to an `LVCompare.exe` command plan with explicit staged VI paths and explicit `-lvpath`. | The authoritative research keeps LVCompare as the fallback engine and requires explicit LabVIEW version selection where possible. | The execution-plan builder returns an `LVCompare.exe` command line using the staged revision files plus explicit `-lvpath`. | Unit test |
| VHS-REQ-146 | The runtime architecture and execution path shall treat a `labview2026q1` Windows container provider as the preferred Windows 64-bit report-execution isolation path for extension users when host-native LabVIEW 2026 64-bit session collision is possible, while keeping Windows 32-bit execution host-native. | Extension users may already have LabVIEW 2026 64-bit open on the host, and author direction requires the product to avoid ambient host-session collisions rather than rely on users to close or reconfigure their host setup. | Architecture records and the live runtime executor both preserve the Windows split: x64 prefers the isolated container provider when available, while x32 remains host-native. | ADR, documentation review, and smoke proof |
| VHS-REQ-147 | The host-native report-execution runner shall stage the selected left and right revision blobs to the governed staged filenames before launching any NI comparison tool. | The planned command line points at staged revision paths, and execution must not depend on ambient repo working files. | A ready runtime execution writes the selected left and right revision blobs to the governed staged paths before command launch. | Unit test |
| VHS-REQ-148 | The host-native report-execution runner shall execute the governed command plan exactly, retain stdout/stderr artifacts, and record exit code plus duration for the attempted NI tool invocation. | Developers need explicit execution evidence and troubleshooting facts rather than a silent success/fail surface. | A runtime execution attempt writes stdout/stderr artifacts, records the executed command, exit code, duration, and preserves those facts in the retained packet metadata. | Unit test |
| VHS-REQ-149 | The host-native report-execution runner shall fail closed with explicit retained failure reasons when staged blob preparation fails, command launch fails, the NI tool exits nonzero, or no governed report HTML is produced. | Execution failures should be diagnosable without rereading raw logs or guessing what went wrong. | Runtime execution records stable failure reasons such as staged-blob failure, spawn failure, nonzero exit, or missing report output, and the retained packet shows `runtimeExecutionState: failed`. | Unit test |
| VHS-REQ-150 | The retained comparison-report packet shall render a developer-facing execution summary including command, report availability, failure reason, exit code, duration, and stdout/stderr artifact paths. | The developer experience surface should make runtime behavior understandable without requiring direct metadata inspection. | Packet HTML includes an execution-summary section with command, report-exists fact, failure reason, exit code, duration, and artifact paths. | Unit test |
| VHS-REQ-151 | The comparison-report action shall open the retained packet artifact and surface execution-state facts, while only treating the governed `diff-report-*.html` path as present when actual NI output exists. | The extension should not imply that a generated NI report exists merely because a packet artifact was persisted. | The action returns packet-artifact path plus runtime execution state, and integration proof shows the packet artifact always exists while the governed report file is only read when execution retained `reportExists: true`. | Unit and integration tests |
| VHS-REQ-152 | The repo shall provide a canonical comparison-report smoke command for `HARNESS-VHS-001` that runs preflight, runtime selection, retained packet persistence, and attempted NI report execution on the latest comparable retained revision pair. | The active runtime-execution tranche needs a real-history smoke lane that proves the governed report path beyond mocked unit coverage. | `npm run harness:report:smoke` writes retained JSON, Markdown, and HTML smoke artifacts for the canonical harness and records the selected/base pair plus report outcome. | Smoke test |
| VHS-REQ-153 | The canonical comparison-report smoke artifacts shall retain factual report-execution evidence including report status, runtime execution state, provider, engine, blocked/failure reason, generated-report presence, and packet/report/metadata paths. | Developers need a local evidence pack for live report generation attempts without reopening raw packet metadata manually. | The retained smoke report surfaces those runtime facts in JSON, Markdown, and HTML. | Smoke test and unit test |
| VHS-REQ-154 | The canonical comparison-report smoke CLI shall accept explicit runtime-selection overrides for platform, preferred bitness, LabVIEW CLI path, LabVIEW executable path, and LVCompare path. | Live NI report proof on developer machines should not rely on ambient defaults when tool locations or host platform need to be forced explicitly. | The CLI parses those overrides deterministically and forwards them into runtime selection for the smoke run. | Unit test |
| VHS-REQ-155 | The runtime locator shall retain actionable diagnostic notes when report generation is blocked because the LabVIEW runtime or comparison tool is missing. | A blocked-runtime fact should tell the developer what to configure next instead of only naming the missing component. | Missing-runtime/tool selections retain notes pointing to `viHistorySuite.labviewExePath`, `viHistorySuite.labviewCliPath`, or `viHistorySuite.lvComparePath` as appropriate. | Unit test |
| VHS-REQ-156 | The canonical comparison-report smoke artifacts shall retain runtime diagnostic notes alongside blocked or failed report-generation facts. | The live smoke packet should remain actionable without reopening the underlying packet metadata or reading raw logs. | The smoke JSON, Markdown, and HTML outputs include the runtime notes from the selected runtime locator result. | Unit test and smoke test |
| VHS-REQ-157 | When a Windows report runtime is executed from a non-Windows Node host, the runtime executor shall mirror staged VI inputs and report output into a Windows-accessible interop workspace and normalize governed executable and path arguments into Windows-native form before NI tool launch. | The authoritative research permits SSH/WSL/devcontainer execution, but Windows NI tools cannot consume Linux-only paths. | A `win32` runtime launched from a non-Windows host invokes NI tooling with Windows-native executable, VI, report, and LabVIEW path arguments while preserving the governed packet/report artifacts. | Unit test |
| VHS-REQ-158 | The canonical comparison-report smoke runner shall supply a Windows-accessible interop workspace for explicit `win32` proof runs from non-Windows hosts and shall fail closed with an explicit runtime failure if that bridge is unavailable. | The live smoke lane should prove Windows-host report execution truthfully on mixed-host developer setups instead of depending on ambient Linux paths that NI tools cannot read. | A `win32` smoke run from a non-Windows host forwards an interop workspace root into runtime execution, and missing interop availability yields a stable retained failure reason rather than a hung or ambiguous run. | Unit test and smoke test |
| VHS-REQ-159 | The runtime executor shall capture the LabVIEW CLI diagnostic log as a governed artifact when the CLI reports a log file path, and it shall retain the source path plus copied artifact path in the packet metadata. | NI CLI runtime failures should be diagnosable from governed artifacts instead of relying on ephemeral `%TEMP%` files outside workspace evidence. | When stdout reports a CLI log path, the executor copies that log into the governed report directory and retains both the original source path and the artifact path in metadata and HTML. | Unit test |
| VHS-REQ-160 | The runtime executor and canonical smoke artifacts shall classify the retained NI CLI diagnostic log when it shows that `-LabVIEWPath` was ignored and the last-used LabVIEW was selected instead. | The current live runtime seam is specifically about explicit LabVIEW version selection being ignored, and that should be surfaced as a first-class factual diagnosis. | A retained CLI log containing the documented “LabVIEWPath command line argument is not passed. Using last used LabVIEW” message yields a stable diagnostic reason and explanatory notes in the packet and smoke artifacts. | Unit test and smoke test |
| VHS-REQ-161 | The comparison-report action and history-panel action tracking shall surface retained runtime diagnostic reason, diagnostic notes, and copied diagnostic-log artifact path whenever NI runtime execution records those facts. | The extension should not force a developer to open packet metadata just to understand what the retained NI runtime diagnosis was. | An opened comparison-report action result and the history-panel tracker both retain runtime diagnostic facts when the packet metadata contains them. | Unit test |
| VHS-REQ-162 | The comparison-report panel webview shall render a developer-facing runtime diagnosis summary including generated-report presence, blocked reason, runtime failure reason, diagnostic reason, and retained diagnostic notes. | Failed NI report attempts should be actionable from the primary extension surface rather than requiring raw-log inspection. | The comparison-report panel status block shows report presence plus actionable blocked/failure/diagnostic details and retained notes when available. | Unit test |
| VHS-REQ-163 | The runtime executor shall distinguish whether an ignored `-LabVIEWPath` defaulted to the same LabVIEW executable or to a different one, and it shall retain that distinction in the diagnostic reason and notes. | An ignored explicit selection is materially different when the fallback still lands on the intended executable versus when it switches to a different installed LabVIEW. | The retained NI diagnostic reason differentiates matched-selection from diverged-selection cases and includes the intended explicit LabVIEW path when the fallback diverges. | Unit test |
| VHS-REQ-164 | The runtime executor shall classify a LabVIEW CLI nonzero exit with no generated report, empty stderr, and stdout limited to the CLI diagnostic-log banner as a dedicated failure reason instead of the generic nonzero-exit bucket. | The current Windows-host NI seam is narrower than a generic subprocess error, and the retained packet should say so explicitly. | A log-only, no-report, nonzero LabVIEW CLI run yields a stable failure reason and an explanatory retained note describing that stdout only advertised the diagnostic log path. | Unit test |
| VHS-REQ-165 | When the retained LabVIEW CLI diagnostic log does not report successful LabVIEW launch, the runtime executor shall retain that absence as an explicit diagnostic note. | The current live Windows-host seam depends partly on what the NI diagnostic log does not confirm, and that absence should be preserved instead of inferred later from memory. | Diagnostic notes explicitly state when the retained CLI log never reported successful LabVIEW launch before exit. | Unit test |
| VHS-REQ-166 | The canonical comparison-report smoke artifacts shall surface the retained runtime stdout and stderr artifact paths when report execution is attempted. | Developers reviewing a failed NI proof should be able to navigate to the retained command streams directly from the smoke packet instead of reopening packet metadata manually. | Smoke JSON, Markdown, and HTML render the runtime stdout/stderr artifact paths when available. | Unit test |
| VHS-REQ-167 | When a Windows comparison-report execution observes the LabVIEW CLI diagnostic-log banner, the runtime executor shall retain a governed process-observation artifact capturing LabVIEW-related process names, PIDs, and observation time from the active Windows host. | The current NI seam must distinguish what was actually observed around CLI launch instead of inferring launch behavior from stderr absence or later Task Manager inspection. | A runtime-process-observation artifact is persisted under the governed report directory and packet metadata records whether `LabVIEW.exe`, `LabVIEWCLI.exe`, or `LVCompare.exe` were observed. | Unit test |
| VHS-REQ-168 | The retained comparison-report packet, comparison-report panel, and canonical comparison-report smoke artifacts shall surface the retained process-observation artifact path plus observed LabVIEW-related process facts when those observations were captured. | Developers need the process-observation facts on the primary review surfaces, not only in a raw JSON side artifact. | Packet HTML, comparison-report panel HTML, and smoke JSON/Markdown/HTML render the observation artifact path, observed process names, and explicit yes/no facts for observed `LabVIEW.exe`, `LabVIEWCLI.exe`, and `LVCompare.exe`. | Unit test |
| VHS-REQ-169 | The retained comparison-report packet, comparison-report panel, and canonical comparison-report smoke artifacts shall surface the observation timestamp and trigger so observed-process facts remain explicitly scoped to the captured snapshot rather than the entire runtime. | Without the observation scope, a developer can overread a single process snapshot as a claim about the whole NI execution. | Packet HTML, comparison-report panel HTML, and smoke JSON/Markdown/HTML render the observation `capturedAt` value and trigger such as `cli-log-banner` whenever a process-observation artifact is retained. | Unit test |
| VHS-REQ-170 | When a process-observation artifact is retained, the runtime executor shall derive scoped diagnostic notes that summarize which LabVIEW-related processes were observed and which key processes were absent at that retained snapshot. | The retained packet should not force the developer to infer process meaning from raw booleans when the runner can summarize the same snapshot factually. | Runtime diagnostic notes include a snapshot-scoped summary of observed LabVIEW-related processes and explicitly note when `LabVIEW.exe` or `LVCompare.exe` were not observed at the retained observation trigger. | Unit test |
| VHS-REQ-171 | When a log-only nonzero LabVIEW CLI failure also retains a `cli-log-banner` snapshot where `LabVIEWCLI.exe` was observed but `LabVIEW.exe` was not observed, the runtime executor shall classify that as a narrower dedicated failure reason rather than the generic log-only nonzero bucket. | The live Windows x86 seam is now narrower than a generic log-only CLI failure, and the retained failure reason should reflect the stronger snapshot evidence. | Runtime execution records a dedicated failure reason for the banner-snapshot-without-LabVIEW case and retains an explanatory note describing that scoped observation. | Unit test |
| VHS-REQ-172 | When a Windows report execution retains a `cli-log-banner` snapshot, the runtime executor shall also retain a second governed process snapshot at command exit whenever observation capture remains available. | A single banner-time snapshot can still overstate uncertainty about what happened later in the NI runtime lane; retaining the exit snapshot narrows that seam without guessing. | The process-observation artifact retains both `cli-log-banner` and `process-exit` snapshots for observed Windows report runs, and diagnostic notes include both scopes when both were captured. | Unit test |
| VHS-REQ-173 | When a log-only nonzero LabVIEW CLI failure retains both a `cli-log-banner` snapshot and a `process-exit` snapshot where `LabVIEWCLI.exe` was observed but `LabVIEW.exe` was not observed in either snapshot, the runtime executor shall classify that as a stricter dedicated failure reason than the banner-only case. | If LabVIEW remains absent through exit across both retained snapshots, the runtime seam is narrower than “not seen at banner” and should be reflected explicitly in the retained failure reason. | Runtime execution records a dedicated failure reason for the no-LabVIEW-through-exit case and retains an explanatory note describing both snapshot scopes. | Unit test |
| VHS-REQ-174 | The retained comparison-report packet HTML shall surface exit-snapshot process-observation facts when a governed `process-exit` snapshot was retained. | Developers reviewing the stored packet should not need to open the raw observation JSON to see that the exit snapshot captured no LabVIEW-related processes. | Packet HTML renders the exit snapshot timestamp, trigger, observed process names, and explicit yes/no facts for observed `LabVIEW.exe`, `LabVIEWCLI.exe`, and `LVCompare.exe` at exit. | Unit test |
| VHS-REQ-175 | The comparison-report action result and panel HTML shall surface exit-snapshot process-observation facts when a governed `process-exit` snapshot was retained. | The primary VS Code comparison-report surface should expose the full retained runtime observation set, not only the banner snapshot. | Action results and panel HTML render the exit snapshot timestamp, trigger, observed process names, and explicit yes/no facts for observed `LabVIEW.exe`, `LabVIEWCLI.exe`, and `LVCompare.exe` at exit. | Unit test |
| VHS-REQ-176 | The canonical comparison-report smoke JSON, Markdown, and HTML artifacts shall surface exit-snapshot process-observation facts when a governed `process-exit` snapshot was retained. | The canonical smoke packet is the main retained runtime proof surface and must reflect the full retained observation window directly. | Smoke JSON, Markdown, and HTML render the exit snapshot timestamp, trigger, observed process names, and explicit yes/no facts for observed `LabVIEW.exe`, `LabVIEWCLI.exe`, and `LVCompare.exe` at exit. | Unit test |
| VHS-REQ-177 | When comparison-report generation returns retained runtime diagnostics and process-observation facts, `openViHistoryCommand` shall propagate those fields into the last action summary recorded for the active history panel. | The driver seat for developer review is the active VS Code session, and the panel tracker should retain the same runtime facts that were already surfaced on the report action result. | The last panel action summary records runtime diagnostic paths, banner snapshot facts, and exit snapshot facts from a successful comparison-report action result. | Unit test |
| VHS-REQ-178 | The direct runtime-command observation runner shall fail closed if exit-snapshot process observation errors after a banner snapshot has already been retained. | Returning a partial runtime result after the governed exit observation fails would undercut the integrity of the retained observation window. | The observed command runner rejects with the exit-observation error instead of returning a partial result when `process-exit` capture fails after a retained `cli-log-banner` snapshot. | Unit test |
| VHS-REQ-179 | The comparison-report action result and panel HTML shall surface the retained runtime executable, runtime arguments, and diagnostic-log source path when those runtime facts are available. | Developers should not need to reopen packet metadata to see the exact NI command surface that was attempted on the active panel path. | Action results and panel HTML render the runtime executable, rendered argument string, and diagnostic-log source path whenever those runtime facts were retained. | Unit test |
| VHS-REQ-180 | The canonical comparison-report smoke JSON, Markdown, and HTML artifacts shall surface the retained runtime executable, runtime arguments, and diagnostic-log source path when those runtime facts are available. | The canonical smoke packet should act as a standalone runtime-proof artifact, including the exact command inputs that were attempted. | Smoke JSON, Markdown, and HTML render the runtime executable, rendered argument string, and diagnostic-log source path whenever those runtime facts were retained. | Unit test |
| VHS-REQ-181 | When comparison-report generation returns a retained runtime executable, runtime arguments, or diagnostic-log source path, `openViHistoryCommand` shall propagate those facts into the last action summary recorded for the active history panel. | The active-session tracker should retain the same command-surface facts that the action result already exposes so local debugging does not depend on hidden metadata. | The last panel action summary records the runtime executable, runtime arguments, and diagnostic-log source path from a successful comparison-report action result. | Unit test |
| VHS-REQ-182 | The repository shall provide a committed current-state entrypoint that identifies the authoritative research stack, the committed capability state, the active development queue, and the primary regenerated evidence paths. | Future maintainers and LLMs should not need chat history or Git archaeology to understand what the repo already does and what is active now. | A committed `docs/product/current-state.md` lists the authority stack, implemented-versus-partial capability surfaces, active tranches, and the local evidence paths that can be regenerated. | Documentation review |
| VHS-REQ-183 | The repository shall provide a committed machine-friendly research-implementation index that maps authoritative research surfaces to implemented, partial, or queued repo status with concrete evidence pointers. | Research-driven implementation should be inspectable from the repo itself instead of being implicit across scattered docs and code. | A committed `docs/research/authoritative/research-implementation-index.json` maps research-driven capability surfaces to status, evidence files, requirement IDs, and next moves. | Documentation review |
| VHS-REQ-184 | The repository entrypoint documentation shall direct future readers to the authoritative research stack, the research-alignment matrix, the current-state surface, and the active development queue. | An outdated README is a weak design because it hides the real authority and active program state from future readers. | `README.md` links to the authoritative research stack, `docs/research/authoritative/research-alignment.md`, `docs/product/current-state.md`, and `docs/product/development-queue.json` as the primary reading order. | Documentation review |
| VHS-REQ-185 | The repository shall model a forward-looking research infrastructure that explains how authoritative research becomes implementation status, queued product work, epics, ADRs, and governed requirements. | Future research rounds should be converted into product work through a repeatable repo-native control plane instead of conversational memory. | A committed research-infrastructure document links the authority stack, implementation index, alignment matrix, development queue, epics, ADRs, and governed implementation artifacts. | Documentation review |
| VHS-REQ-186 | The repository shall model a first-class multi-report developer dashboard epic that concentrates multiple VI Comparison Reports for one VI across at least three commits. | The product direction needs an explicit forward-looking target for improving human review decisions beyond one pairwise comparison report. | A committed epic defines the dashboard outcome, scope, child slices, and exit criteria for a one-VI, three-plus-commit review scenario. | Documentation review |
| VHS-REQ-187 | The architecture shall define the multi-report developer dashboard as a chronology-aware review surface that preserves multiple retained pairwise comparison-report packets and their raw evidence. | The dashboard direction is architectural, not only product wording, because it changes the review surface and report-subsystem boundaries. | A committed ADR and architecture overview describe the dashboard as a first-class review surface for commit-window decisions and show its relationship to the report subsystem. | Documentation review |
| VHS-REQ-188 | The repository shall model a review-scenario registry for dashboard-driven VI review, including at least one canonical scenario that requires one VI, at least three commits, and at least two comparison pairs. | A dashboard without explicit review scenarios risks becoming a generic UI instead of a bounded human decision-support system. | A committed scenario registry defines `SCENARIO-VHS-001` with harness, VI target, commit-window minimum, and comparison-pair minimum. | Documentation review |
| VHS-REQ-189 | The repository shall provide a separate human decision-record template for dashboard-driven review outcomes so retained dashboard evidence and reviewer judgment are not conflated. | Human decision making should be enhanced by the dashboard, not silently replaced or merged into the raw evidence packet. | A committed decision-record template captures scenario ID, commit window, dashboard packet, reviewer outcome, confidence, and follow-up fields separately from the dashboard packet. | Documentation review |
| VHS-REQ-190 | The architecture shall define the multi-report dashboard as concentration-first for high-volume open-source VI review so the default workflow is not linear inspection of every individual comparison report. | The smarter product direction is to reduce reviewer wear while preserving raw evidence, not to build a gallery that still forces report-by-report inspection. | A committed ADR and epic state that the dashboard concentrates many pairwise report facts into chronology-aware review cues and reduces the default need to open every individual report. | Documentation review |
| VHS-REQ-191 | The review-scenario registry shall model at least one high-volume open-source VI review scenario where a dashboard helps a human reviewer triage many modifications on one VI. | The product should be explicit that its long-term users may review large open-source change sets, not only minimal three-commit cases. | A committed scenario registry defines a high-volume open-source review scenario with concentration, prioritization, and drill-down expectations. | Documentation review |
| VHS-REQ-192 | The architecture shall preserve drill-down from concentrated dashboard cues back to the underlying raw packet and raw VI Comparison Report artifacts. | Concentration without drill-down would weaken trust because a human reviewer could not verify the summarized cues against retained evidence. | A committed ADR and epic both preserve direct navigation from dashboard cues to raw packet/report artifacts. | Documentation review |
| VHS-REQ-193 | The canonical comparison-report smoke CLI shall accept an explicit `--engine <labview-cli|lvcompare>` override so parity probing stays inside the governed smoke lane. | The live runtime seam should be narrowed through governed parity probes, not by ad hoc local command variants that future maintainers might miss. | The smoke CLI usage and parser accept `--engine labview-cli` and `--engine lvcompare`, and the parsed value is forwarded into the smoke runner. | Unit test |
| VHS-REQ-194 | The canonical comparison-report smoke runner shall fail closed with an explicit blocked reason when a requested report engine override is unavailable on the active host. | An explicit parity probe should not silently fall back to a different engine, because that would blur the runtime seam instead of narrowing it. | Requesting `lvcompare` or `labview-cli` when that engine is unavailable yields an explicit blocked reason such as `requested-lvcompare-not-available` or `requested-labview-cli-not-available`. | Unit test |
| VHS-REQ-195 | The canonical comparison-report smoke runner shall persist the effective runtime engine and an explicit override note into the retained packet path when an engine override is requested. | A governed parity probe should leave durable evidence of what engine was actually requested and selected. | The retained smoke packet includes the effective runtime engine and notes such as `Requested runtime engine override: lvcompare.` when an override is used. | Unit test |
| VHS-REQ-196 | The runtime executor shall clear stale diagnostic-log artifacts when the current comparison-report run does not produce or retain a new diagnostic log. | A parity probe should not inherit a previous run's CLI diagnostic artifact, because that would misstate what the current engine actually produced. | A run without a current diagnostic log leaves `diagnosticLogArtifactPath` unset for the current packet and removes any stale retained diagnostic-log artifact from the governed report directory. | Unit test |
| VHS-REQ-197 | The runtime executor shall classify an `lvcompare` exit code `0` with no generated governed report file as a dedicated failure reason instead of the generic missing-report bucket. | The raw engine parity lane should surface a narrower factual seam than the generic no-report bucket when `LVCompare.exe` returns successfully but still does not produce the expected output. | A retained runtime execution using `lvcompare` records `lvcompare-exited-zero-without-report` and a matching explanatory note when exit code is `0` and no governed report file exists. | Unit test |
| VHS-REQ-198 | The direct runtime-command observation runner shall capture a governed `process-spawn` snapshot and a governed `process-exit` snapshot for `lvcompare` parity probes on Windows runtime paths. | The raw engine lane emits no LabVIEW CLI banner, so `lvcompare` needs its own deterministic process-observation trigger in order to retain process truth. | An `lvcompare` run with observation enabled records a `process-spawn` snapshot before command exit and a `process-exit` snapshot afterward. | Unit test |
| VHS-REQ-199 | The direct runtime-command observation runner shall fail closed when `lvcompare` spawn-time process observation capture errors. | Returning a partial raw-engine result after spawn observation fails would weaken the parity-proof lane. | An observed `lvcompare` command rejects when `process-spawn` observation capture fails instead of returning a partial runtime result. | Unit test |
| VHS-REQ-200 | The comparison-report subsystem shall archive retained pairwise report evidence by `reportType + baseHash + selectedHash` so multiple report attempts for one VI can coexist without overwriting. | A first-class multi-report dashboard needs a durable per-pair evidence layer, while the current mutable pairwise packet path is insufficient for commit-window concentration. | After report generation is requested, the extension retains a pair-scoped archive root containing the packet and source record for that selected/base pair. | Unit test |
| VHS-REQ-201 | The pair archive shall retain packet HTML, metadata, runtime artifacts, and copied report assets when they exist. | Concentrating multiple NI reports into one dashboard requires more than a packet pointer; it requires the underlying report facts and image assets to remain available per pair. | A pair archive contains copied packet/metadata/runtime artifacts and the report asset directory when the source report provided one. | Unit test |
| VHS-REQ-202 | The VI History review surface shall expose `Open dashboard` only when the retained commit window contains at least three commits and shall keep the action disabled for smaller windows. | The dashboard is defined as a commit-window review surface, not a replacement for the simple two-commit review flow. | The history panel renders an enabled `Open dashboard` action only when the retained window has at least three commits, and the copied review packet states whether the dashboard is available. | Unit and integration tests |
| VHS-REQ-203 | The dashboard builder shall derive its review window from retained commit pairs and shall surface missing archived pairs explicitly instead of hiding them. | High-volume review concentration becomes misleading if the dashboard silently omits pairs that were never archived or were not yet generated. | A dashboard entry exists for each retained adjacent commit pair, with `archived` or `missing` status recorded explicitly. | Unit test |
| VHS-REQ-204 | The dashboard builder shall parse retained VI Comparison Report HTML to collect report title, compared VI paths, overview images, included attributes, and detailed information sections. | The new dashboard must recollect all report information into one surface rather than summarizing only status metadata. | A stored archived NI report yields parsed report title, first/second VI paths, overview image references, included-attribute facts, and detailed information sections in the dashboard packet. | Unit test |
| VHS-REQ-205 | The dashboard builder shall copy recollected report overview image assets into a dashboard-scoped artifact directory and render them in the concentrated dashboard HTML. | Concentrating multiple NI reports into one HTML requires image assets to remain available from one review surface instead of forcing linear raw-report inspection. | Persisting a dashboard copies overview image assets into a dashboard asset directory and renders them in the dashboard HTML. | Unit test |
| VHS-REQ-206 | The dashboard subsystem shall persist a concentrated dashboard JSON packet and dashboard HTML under workspace-scoped `context.storageUri/dashboards/<repoId>/<fileId>/<windowId>/`. | The multi-report dashboard should be a retained evidence surface, not only an in-memory panel render. | Building a dashboard writes both `dashboard.json` and `dashboard.html` under a deterministic workspace-scoped dashboard directory. | Unit test |
| VHS-REQ-207 | The extension shall surface the concentrated dashboard through a dedicated review-dashboard panel action and shall retain pair-count, archived-pair-count, and missing-pair-count facts in the panel action summary. | Human reviewers need the dashboard surfaced directly in the extension, and future automation needs retained facts about what the dashboard actually concentrated. | Triggering `Open dashboard` opens a dedicated dashboard panel and records pair-count, archived-pair-count, and missing-pair-count in the last action summary. | Unit test |
| VHS-REQ-208 | The repo entrypoints and research-control-plane surfaces shall state that the multi-report dashboard is partially implemented through pair-archive retention, NI report parsing, and concentrated dashboard HTML. | Future LLMs and human maintainers should be able to tell from repo-native docs that dashboard work is no longer only modeled intent. | README, current-state, research alignment, implementation index, development queue, and architecture records all identify the dashboard as partially implemented and active. | Documentation review |
| VHS-REQ-209 | The repository shall delete consumed ad hoc research-round artifacts after their findings have been normalized into committed implementation, queue, ADR, and requirement surfaces. | Retired research should not remain available as a stale active authority surface that can cause future hallucinations or contradictions. | The consumed research-round files are absent from `docs/research/authoritative/`, and repo entrypoints describe consumed rounds as deleted after normalization. | Documentation review |
| VHS-REQ-210 | The committed repo entrypoints and research-control-plane surfaces shall stop referencing deleted consumed research rounds directly and shall instead point future readers to the current state, alignment, queue, and next research prompt. | Future maintainers and LLMs should derive what to do next from live product/control-plane surfaces rather than stale retired research artifacts. | README, current-state, research alignment, implementation index, and research infrastructure no longer reference a deleted active-round artifact and instead surface the current control plane plus next research prompt. | Documentation review |
| VHS-REQ-211 | The repository shall provide a future-facing next research prompt that starts from the currently implemented dashboard, runtime-proof, and decision-support baseline rather than restating already-consumed unresolved workstreams. | Once a research round is retired, the next prompt should drive only genuinely new research and avoid reintroducing solved or normalized work as if it were unresolved. | `docs/research/authoritative/next-research-prompt.md` targets future dashboard concentration, mixed-provider contribution, runtime doctor UX, and decision-support workstreams from the current implemented baseline. | Documentation review |
| VHS-REQ-212 | The concentrated dashboard shall surface provider provenance per archived pair, including runtime provider, engine, platform, and preferred bitness, so mixed-provider commit windows remain reviewable without ambiguity. | Future mixed-provider dashboard windows will become misleading if pair entries do not preserve how each retained report was produced. | Every archived dashboard entry retains provider provenance fields derived from the archived packet and renders them in the dashboard HTML. | Unit test |
| VHS-REQ-213 | The concentrated dashboard summary shall surface only metadata that exists in retained VI Comparison Reports, including metadata-backed pair count, provider coverage, overview section and image counts, included-attribute counts, and detailed-information section and item counts. | High-volume dashboard review should concentrate actual comparison-report metadata instead of ranking-style cues or inferred priority. | The stored dashboard record and HTML render summary counts derived from retained NI report metadata fields only. | Unit test |
| VHS-REQ-214 | The concentrated dashboard HTML shall render per-pair VI Comparison Report metadata from retained NI report HTML, including report title, generation time, compared VI paths, overview section captions with image counts, included attributes, and detailed-information headings and items when present. | The expert review surface should concentrate the actual metadata emitted by NI comparison reports instead of forcing raw-report inspection or inventing surrogate cues. | Each dashboard entry renders the retained NI report metadata fields for that pair, or states that no retained comparison-report metadata is available for that pair. | Unit test |
| VHS-REQ-215 | The dashboard action shall open archived HTML artifacts in dedicated review panels and archived JSON artifacts in the editor, while failing closed on artifact paths outside workspace-scoped extension storage. | Drill-down should be useful to reviewers but must not create an unbounded artifact-opening surface. | Dashboard artifact actions open packet/report HTML in dedicated panels, open JSON artifacts in the editor, and reject paths outside `context.storageUri`. | Unit test |
| VHS-REQ-216 | The repository shall consume incoming future-facing research by normalizing its actionable findings into the committed queue, epic, ADR, requirement, and implementation surfaces, then deleting the incoming research artifact. | Incoming research should not linger as an ungoverned parallel plan once it has been adopted into the product program. | Development queue, epic, ADR, current-state, alignment, implementation index, and requirements reflect the consumed future-facing dashboard research, and the incoming research file is absent. | Documentation review |
| VHS-REQ-217 | The Windows 64-bit container runtime executor shall root container `TEMP` and `TMP` under the governed report workspace so the NI CLI diagnostic log is emitted in a host-readable location that can be copied back into retained artifacts. | The live container proof depends on relocating transient NI CLI logs out of opaque container temp space and into a governed bind-mounted workspace. | Container execution injects container-local `TEMP` and `TMP` under the governed report workspace, and a reported CLI log path is copied into the retained report directory. | Unit test |
| VHS-REQ-218 | The Windows 64-bit container runtime executor shall apply startup mitigation for LabVIEW CLI connectivity, including CLI INI timeout tuning to 180 seconds, headless LabVIEW prelaunch, and one bounded retry after a startup-connectivity failure. | The live NI container proof required explicit mitigation of startup-connectivity failures before `CreateComparisonReport` would complete reliably. | Container execution patches LabVIEW CLI timeout settings, prelaunches LabVIEW headlessly, and retries once after the governed startup-connectivity failure before retaining the final runtime result. | Unit test |
| VHS-REQ-219 | The Windows 64-bit container runtime executor shall preserve overwrite semantics across repeated report-generation runs and shall copy both the generated report HTML and its sibling `_files` asset directory back into the governed report directory when runtime execution succeeds. | Reliable expert review depends on regenerated NI HTML staying current across repeated runs and on accompanying image/style assets remaining available to the extension and dashboard surfaces. | A repeated successful container execution preserves overwrite behavior and retains both `diff-report-*.html` and `diff-report-*_files/` under the governed report directory. | Unit and smoke tests |
| VHS-REQ-220 | The canonical comparison-report smoke lane shall retain a succeeded Windows 64-bit container-backed NI proof, including the generated report HTML, copied `_files` asset directory, runtime stdout/stderr, and copied diagnostic-log artifact. | The repo needs a first-class live proof that the preferred Windows 64-bit provider actually produces a usable NI report on the canonical harness instead of only modeling the path architecturally. | `npm run harness:report:smoke` on the canonical harness retains JSON, Markdown, and HTML smoke artifacts that show a succeeded container-backed runtime execution with report, assets, and runtime stream artifacts present. | Smoke test |
| VHS-REQ-221 | The dashboard packet shall retain an explicit per-pair evidence state that distinguishes missing archives, archived generated-report pairs, archived blocked pairs, archived failed pairs, and archived pairs that retained no generated report. | A concentration-first dashboard becomes ambiguous if a reviewer has to infer pair completeness from several loosely related fields instead of one governed state. | Every dashboard entry records one explicit evidence-state value derived from archive presence, retained runtime state, and retained generated-report existence. | Unit test |
| VHS-REQ-222 | The dashboard summary shall retain explicit window-completeness facts, including represented pair count, completeness state, and the exact missing, blocked, and failed pair identifiers, while keeping the HTML focused on concentrated comparison metadata rather than pair-id exception lists. | Multi-report review still needs deterministic completeness accounting, but the primary dashboard HTML should stay centered on comparison-report metadata instead of exception bookkeeping. | The stored dashboard JSON retains represented-pair count, completeness state, and pair-id lists for missing, blocked, and failed entries, while the HTML emphasizes concentrated comparison metadata summaries. | Unit test |
| VHS-REQ-223 | The dashboard rebuild path shall remove stale dashboard asset artifacts before regenerating the concentrated dashboard from retained pair archives. | A rebuild that leaves copied images from an earlier archive state behind is nondeterministic and can misstate current retained evidence. | Rebuilding the same dashboard window after retained report content changes removes stale copied assets and regenerates the dashboard asset directory only from the current retained archives. | Unit test |
| VHS-REQ-224 | The comparison-report subsystem shall derive a compact runtime-doctor summary from retained runtime-selection and runtime-execution facts, including chosen provider, chosen engine, platform, preferred bitness, selected runtime tools, failure or blocked reasons when present, and one bounded next action. | Users should not need to reconstruct the runtime situation from scattered fields and raw logs when troubleshooting report generation. | The retained packet/runtime metadata includes a deterministic runtime-doctor summary derived from existing execution facts only. | Unit test |
| VHS-REQ-225 | The comparison-report packet HTML and live comparison-report panel shall render the retained runtime-doctor summary so runtime/provider troubleshooting remains understandable without opening raw logs first. | The first runtime-doctor slice must be visible on the main comparison-report surfaces, not hidden only in JSON metadata. | The stored packet HTML and extension panel both render the retained runtime-doctor summary lines when present. | Unit tests |
| VHS-REQ-226 | The runtime locator shall retain structured provider-decision facts covering selected and rejected report-execution providers, including provider identity, outcome, reason, and bounded detail, whenever runtime selection chooses between host-native and Windows-container execution or blocks because no usable provider is available. | Users cannot understand provider choice from scattered notes alone; the selection contract needs first-class provider-decision evidence. | The retained runtime-selection metadata includes structured provider decisions for the relevant provider choices on the active platform. | Unit tests |
| VHS-REQ-227 | The runtime-doctor summary shall surface retained provider-decision facts on the stored packet HTML and live comparison-report panel so users can see why provider alternatives were selected or rejected without reconstructing that logic from runtime notes. | Provider troubleshooting remains ambiguous if chosen and rejected provider reasons stay hidden in selection metadata only. | Runtime-doctor summaries on packet and panel surfaces include provider-decision lines derived from retained runtime-selection facts. | Unit tests |
| VHS-REQ-228 | The comparison-report action shall fail closed with a stable `workspace-untrusted` outcome when comparison-report generation is requested from an untrusted workspace, before preflight, runtime discovery, storage, or NI tool execution occurs. | History panels can outlive trust changes; execution trust must remain a hard runtime gate, not only an initial menu-visibility assumption. | An untrusted comparison-report request returns `workspace-untrusted` and does not invoke preflight, runtime discovery, packet persistence, or panel creation. | Unit test |
| VHS-REQ-229 | The multi-report dashboard action shall fail closed with a stable `workspace-untrusted` outcome when dashboard generation is requested from an untrusted workspace, before dashboard build or panel creation occurs. | Dashboard refresh is another long-running evidence-producing action and must honor workspace-trust changes explicitly. | An untrusted dashboard request returns `workspace-untrusted` and does not build the dashboard or open a panel. | Unit test |
| VHS-REQ-230 | The history-panel command flow shall surface stable warnings when comparison-report generation or dashboard generation is blocked by workspace trust after the panel is already open. | A trust-gated refusal should be explicit and actionable even when the initial history panel was opened while the workspace was trusted. | When a panel action returns `workspace-untrusted`, the command flow shows a stable warning and retains the explicit `workspace-untrusted` action outcome. | Unit test |
| VHS-REQ-231 | The comparison-report action shall emit bounded progress updates for retained-pair resolution, preflight validation, runtime selection, packet persistence, NI runtime execution when attempted, evidence archival, and panel opening. | Comparison-report generation is long-running enough that users need staged progress instead of an opaque wait. | A progress-aware comparison-report request receives deterministic stage messages with bounded increments across the governed execution path. | Unit test |
| VHS-REQ-232 | The multi-report dashboard action shall emit bounded progress updates for commit-window preparation, per-pair metadata concentration, retained asset finalization, and dashboard opening. | Dashboard refresh should make its major work stages visible without requiring raw logs or source inspection, especially when many adjacent comparison pairs contribute to one concentrated dashboard. | A progress-aware dashboard request receives deterministic stage messages with bounded increments for commit-window preparation, pair-by-pair concentration, finalization, and panel opening. | Unit test |
| VHS-REQ-233 | The history-panel command flow shall wrap dashboard and comparison-report actions in notification progress with stable titles and cancellation support using the bounded stage updates emitted by those actions. | Progress hooks are not useful unless the main user-facing command flow actually surfaces them and lets users stop long-running work intentionally. | Panel-triggered dashboard and comparison-report actions execute inside notification progress with stable titles and cancellable behavior. | Unit test |
| VHS-REQ-234 | The comparison-report action shall return a stable `cancelled` outcome with a bounded cancellation stage and any already-retained packet/report artifact paths when cancellation is observed after packet persistence or later stages. | Cancellation should preserve the evidence already retained instead of discarding the partial run or forcing the user to infer what survived. | A cancelled comparison-report request after packet persistence returns `cancelled`, a stable stage identifier, and retained packet/report metadata while avoiding panel creation. | Unit test |
| VHS-REQ-235 | The multi-report dashboard action shall return a stable `cancelled` outcome with a bounded cancellation stage and any already-retained dashboard artifact paths when cancellation is observed after dashboard build. | Dashboard refresh cancellation should preserve the concentrated artifact that was already built instead of silently throwing away useful evidence. | A cancelled dashboard request after build returns `cancelled`, a stable stage identifier, retained dashboard paths/counts, and does not open a panel. | Unit test |
| VHS-REQ-236 | The history-panel command flow shall surface stable informational messages and explicit `cancelled` action summaries when dashboard or comparison-report actions are cancelled after retaining partial evidence. | Users need a clear statement that work stopped intentionally and that retained artifacts, if any, remain available. | When a panel action returns `cancelled`, the command flow shows a stable informational message and records the explicit cancellation stage plus retained artifact paths in the tracker summary. | Unit test |
| VHS-REQ-237 | The multi-report dashboard action shall ignore malformed dashboard artifact messages, including unsupported artifact kinds and blank required fields, without opening artifacts or surfacing warnings. | The dashboard webview can emit arbitrary messages, and malformed payloads should fail closed quietly instead of producing side effects or noisy warnings. | Unsupported or malformed dashboard artifact messages do not open panels, do not open editors, and do not surface warnings. | Unit test |
| VHS-REQ-238 | The multi-report dashboard action shall open only governed retained artifact files whose declared kind matches the retained filename contract under workspace-scoped extension storage, rejecting storage-root targets and kind/path mismatches with a stable warning. | Concentration-first review still preserves bounded artifact access, but the action boundary must not trust a webview-supplied kind/path pair more than the governed archive contract allows. | Storage-root targets and kind/path mismatches are rejected with a stable warning, while governed `report-packet.html`, `report-metadata.json`, `source-record.json`, and `*-report-*.html` targets continue to open through the correct surface. | Unit test |
| VHS-REQ-239 | When the Windows 64-bit container report provider runs from a native Windows host, the runtime executor shall use native `powershell.exe` and governed native Windows report/staging paths without requiring a cross-host interop workspace root. | Extension users commonly run VS Code directly on Windows, so the preferred container provider needs governed proof on the native Windows-host lane rather than only on the WSL-style interop lane. | A native-Windows container execution runs through `powershell.exe`, binds the governed native Windows report directory into the container workspace, and succeeds without an `interopWorkspaceRoot`. | Unit test |
| VHS-REQ-240 | When the Windows 64-bit container provider is asked to execute an `lvcompare` parity probe from a non-Windows host, the runtime executor shall rewrite the staged VI paths and `-lvpath` argument into the container workspace and invoke the direct LVCompare command through the governed PowerShell wrapper. | The preferred x64 provider should preserve direct-engine parity probes, not silently become LabVIEWCLI-only once execution moves into the container boundary. | A container-backed `lvcompare` parity run invokes the PowerShell wrapper, rewrites staged VI paths into `C:\\vi-history-suite\\staging\\...`, preserves the governed `-lvpath`, and succeeds when the governed report file is present. | Unit test |
| VHS-REQ-241 | The comparison-report action result and panel HTML shall preserve explicit zero-process observations as factual `none` values when runtime observation was retained but no LabVIEW-related process names were observed. | Silently omitting an empty observed-process list blurs the difference between “no observation retained” and “observation retained with zero matching processes,” which weakens runtime diagnosis. | When runtime execution retains empty `observedProcessNames` or `exitObservedProcessNames`, the action result preserves empty arrays and the panel HTML renders `Observed process names: none` and `Exit observed process names: none`. | Unit test |
| VHS-REQ-242 | When the Windows 64-bit container provider is selected without a configured container image, the runtime executor shall fail closed before command preparation or execution with the explicit reason `windows-container-image-unavailable`. | A selected provider without its governing image is an incomplete runtime choice and should block immediately with a precise reason instead of drifting into a later generic execution failure. | A Windows-container execution without `windowsContainerImage` records `windows-container-image-unavailable`, marks the run as not attempted, and does not invoke the runtime command. | Unit test |
| VHS-REQ-243 | The runtime-diagnostic capture path shall map a container-reported NI CLI log path into host-readable storage only when the reported path remains under the governed mapped runtime root; paths outside that root shall be treated as unreadable. | Container execution should not trust arbitrary runtime-reported paths outside the governed bind-mounted runtime root, because that weakens provenance and can blur whether a retained diagnostic artifact actually came from the active run. | A diagnostic log under the governed mapped runtime root resolves into host-readable storage, while a path outside that runtime root resolves as unreadable and is not treated as a governed host-readable artifact. | Unit test |
| VHS-REQ-244 | The runtime-diagnostic classifier shall retain an explicit launch-success note when the NI CLI diagnostic log reports that LabVIEW launched successfully before the operation failed, even when no `LabVIEWPath` anomaly is present. | Runtime diagnosis should distinguish “LabVIEW never launched” from “LabVIEW launched and the failure happened later” using retained NI evidence, not inference. | A diagnostic log containing `LabVIEW launched successfully.` yields a retained launch-success note without inventing a `LabVIEWPath` anomaly reason. | Unit test |
| VHS-REQ-245 | The runtime executor shall retain explicit `none` process-observation notes when a governed process snapshot is captured but contains zero LabVIEW-related process names. | A retained empty snapshot is different from no snapshot at all, and that distinction matters when diagnosing whether NI tooling launched anything observable. | When runtime execution retains a process snapshot with an empty observed-process list, the retained diagnostic notes state `observed LabVIEW-related processes: none`. | Unit test |
| VHS-REQ-246 | The comparison-report action shall retain a stable `cancelled` result with the `after-archive` stage and preserved blocked-runtime evidence when cancellation is observed after governed archive completion but before the panel opens. | Cancellation after archive is late enough that the user should not lose the retained blocked-runtime packet state or have to infer whether archival already happened. | A blocked-runtime packet cancelled after archive returns `cancelled`, `after-archive`, the preserved blocked reason, and retained packet/report paths while avoiding panel creation. | Unit test |
| VHS-REQ-247 | The comparison-report panel shall render non-empty exit observed process names exactly as retained runtime metadata instead of collapsing them into `none` or omitting them. | Expert users need to see the concrete exit-snapshot process set when NI tooling leaves observable processes behind at exit. | When retained runtime metadata includes non-empty `exitObservedProcessNames`, the comparison-report panel renders them as a joined list on the exit observation line. | Unit test |
| VHS-REQ-248 | The default Windows-container image availability probe shall use native `docker image inspect` on Windows hosts and `cmd.exe /c docker image inspect` from non-Windows hosts, returning `false` rather than throwing when the probe command fails. | Provider selection should rest on governed host-specific image probing rather than implicit assumptions about where Docker is reachable from the extension host. | The default image probe uses `docker` directly on Windows, uses `/mnt/c/Windows/System32/cmd.exe /c docker ...` from non-Windows hosts, and returns `false` on probe failure. | Unit test |
| VHS-REQ-249 | The Windows-container LabVIEW CLI argument rewrite shall ignore caller-supplied `-LabVIEWPath`, ignore caller-supplied `-Headless` values, drop `-c`, preserve other supported arguments, and append the governed `-LabVIEWPath` plus `-Headless true`. | Container execution should not inherit caller-supplied LabVIEW-path or headless flags that can conflict with the governed container runtime contract. | Rewriting LabVIEW CLI args for the Windows-container provider replaces staged/report paths, removes caller `-LabVIEWPath`, removes caller `-Headless` values, drops `-c`, preserves other supported arguments, and appends the governed `-LabVIEWPath` plus `-Headless true`. | Unit test |
| VHS-REQ-250 | The Windows-container LVCompare argument rewrite shall fail closed when fewer than two staged VI paths are supplied and shall preserve additional comparison flags while replacing the staged pair and governed `-lvpath`. | Direct LVCompare parity probes need a deterministic container rewrite contract instead of ad hoc string substitution, especially when preserving report-comparison flags. | Rewriting LVCompare args for the Windows-container provider returns `undefined` when the staged pair is incomplete and otherwise rewrites the staged pair, rewrites `-lvpath`, and preserves additional comparison flags. | Unit test |
| VHS-REQ-251 | The comparison-report action shall return a stable `cancelled` result with the `after-runtime-selection` stage when cancellation is observed after runtime selection but before packet persistence. | Runtime selection can take enough time that cancellation has to be explicit even before a packet exists, otherwise the user has no truthful stage boundary for why execution stopped. | When cancellation is observed after runtime selection and before packet persistence, the action returns `cancelled` with stage `after-runtime-selection`, does not persist a packet, and does not open a panel. | Unit test |
| VHS-REQ-252 | The comparison-report action shall return a stable `cancelled` result with the `after-runtime-execution` stage and preserved runtime-executed packet evidence when cancellation is observed after runtime execution but before archive or panel open. | Users need truthful retained state when they cancel after NI execution has already happened, because the runtime result is materially different from earlier cancellation points. | When cancellation is observed after runtime execution, the action returns `cancelled` with stage `after-runtime-execution`, preserves packet/report metadata plus runtime failure facts, does not archive, and does not open a panel. | Unit test |
| VHS-REQ-253 | The history-panel command flow shall surface a stable warning and retain the explicit `missing-dashboard-storage` action summary when dashboard generation is requested without workspace-scoped storage. | Dashboard refresh can be triggered from an already-open panel, so the command flow must explain storage refusal explicitly instead of leaving the user to infer why no dashboard opened. | When the dashboard action returns `missing-storage-uri`, the command flow shows the stable storage warning and records `missing-dashboard-storage` on the last action summary. | Unit test |
| VHS-REQ-254 | The history-panel command flow shall surface a stable informational message and retain the explicit `insufficient-dashboard-commits` action summary when dashboard generation is requested for a VI with fewer than three retained commits. | Concentration-first dashboard review has a hard minimum history window, and the command flow should retain that refusal explicitly for both the user and the tracker. | When the dashboard action returns `insufficient-commits`, the command flow shows the stable informational message and records `insufficient-dashboard-commits` on the last action summary. | Unit test |
| VHS-REQ-255 | Runtime diagnostic-path mapping shall fail closed when no mapping is configured or when either the diagnostic path or governed runtime root cannot be normalized into comparable Windows paths. | The runtime doctor should not claim a host-readable NI diagnostic log path from incomplete or malformed mapping inputs, because that would overstate evidence availability. | Direct diagnostic-path mapping returns `undefined` when mapping is absent, the diagnostic path is blank, or the governed runtime root is blank. | Unit test |
| VHS-REQ-256 | Governed command-option extraction shall ignore blank option values so runtime diagnosis can fall back to the retained LabVIEW selection instead of treating whitespace as an explicit override. | A whitespace-only `-LabVIEWPath` value is not a real override, and carrying it forward would weaken the runtime-doctor evidence chain. | Command-option extraction returns `undefined` for blank option values and returns the trimmed value for nonblank matches. | Unit test |
| VHS-REQ-257 | Windows-path normalization for runtime execution shall return `undefined` for blank path inputs instead of treating whitespace as a valid Windows or WSL-interoperable path. | Blank path strings should fail closed before provider or diagnostic logic attempts to normalize them into executable or artifact paths. | Windows-path normalization returns `undefined` for blank strings and still normalizes valid Windows or `/mnt/<drive>/...` inputs. | Unit test |
| VHS-REQ-258 | The Windows-container LabVIEW CLI script builder shall set `$labviewPath = $null` when no usable governed LabVIEW executable path is provided and shall emit a quoted explicit path only when the trimmed path is nonblank. | Container execution should not prelaunch or test a whitespace-only LabVIEW path, because that would blur the governed boundary between explicit selection and no-selection. | Building the container LabVIEW CLI script emits `$labviewPath = $null` for blank input and emits the quoted explicit path for nonblank input. | Unit test |
| VHS-REQ-259 | The design gate shall persist a running retained report after each successful step, including the pending next step, so `latest-report.json` and `latest-report.md` stay fresh even if a later step hangs. | Autonomous tranche selection should not depend on the final assurance step completing, because a stuck tail otherwise leaves stale gate evidence. | A successful full gate produces intermediate retained reports marked `running` with the pending next step before the final retained report marked `complete`. | Unit test |
| VHS-REQ-260 | The retained design-gate report surfaces shall expose explicit completion state and pending-step facts. | Future sessions need to distinguish a complete gate from a gate that produced fresh partial evidence but is still waiting on a later step. | The retained design-gate JSON and Markdown surfaces include `completionState` and, for running states, the pending step id and title. | Unit test |
| VHS-REQ-261 | The comparison-report plan builder shall map each governed report format to the exact LabVIEW CLI report-type token: `HTMLSingleFile -> htmlsinglefile`, `HTML -> html`, `XML -> xml`, `PlainText -> plaintext`, and `MicrosoftWord -> microsoftword`. | The extension should not guess NI CLI report-type tokens; each supported output format needs an explicit governed mapping contract. | Building the LabVIEW CLI comparison-report plan emits the exact expected `-ReportType` token for every governed report format. | Unit test |
| VHS-REQ-262 | The comparison-report plan builder shall include a trimmed `-description` argument only when a nonblank description is provided and shall omit `-c` and `-o` when output-directory creation or overwrite are explicitly disabled. | Optional CreateComparisonReport arguments should be governed, because description text and output flags materially change how NI tooling records or writes reports. | Building the LabVIEW CLI comparison-report plan trims a nonblank description into `-description` and omits `-c` plus `-o` when those options are disabled. | Unit test |
| VHS-REQ-263 | The Windows-container command planner shall fail closed when no governed runtime engine is selected or when rewriting the selected engine’s args into container form yields no executable command. | The container provider should not invent a fallback command when the runtime selection or argument rewrite contract is incomplete, because that would blur the governed execution boundary. | Building the Windows-container command plan returns `undefined` when the retained runtime engine is missing or when the selected engine’s rewritten container args are unavailable. | Unit test |
| VHS-REQ-264 | Windows-container execution shall retain separate host-accessible and normalized-Windows temp roots, deriving the Windows temp root from the normalized Windows report directory while preserving the host temp root for filesystem access. | Container execution needs one governed Windows-path normalization boundary while still writing files through the host-accessible filesystem path, especially on non-Windows hosts. | Preparing a non-Windows-host container execution context retains `diagnosticPathMapping.hostRoot` as the host-accessible temp directory, retains `runtimeRoot` as `C:\\vi-history-suite\\container-temp`, and creates the temp directory under the host-accessible path. | Unit test |
| VHS-REQ-265 | Windows-container execution-context preparation shall fail closed with `windows-interop-root-unavailable` when the runtime platform requires Windows interop from a non-Windows host and no interop workspace root is provided. | The container provider cannot stage report and VI artifacts onto a Windows-accessible host path without an explicit interop root. | Preparing a Windows-container execution context on a non-Windows host with no interop root returns a blocked context with `windows-interop-root-unavailable`. | Unit test |
| VHS-REQ-266 | Windows-container execution-context preparation shall fail closed with `windows-path-normalization-failed` when the selected host report directory cannot be normalized into governed Windows form. | Container execution should not proceed when the host report directory cannot be translated into a Windows path for the provider command plan. | Preparing a Windows-container execution context returns a blocked context with `windows-path-normalization-failed` when the host report directory is not Windows-normalizable. | Unit test |
| VHS-REQ-267 | The dashboard action shall return a stable `cancelled` result with the `before-dashboard-build` stage when cancellation is already requested before dashboard concentration begins. | The dashboard command flow needs an explicit earliest cancellation boundary so it does not imply any concentration work happened when none did. | When cancellation is requested before dashboard build starts, the action returns `cancelled` with stage `before-dashboard-build`, does not build the dashboard, and does not open a panel. | Unit test |
| VHS-REQ-268 | The concentrated dashboard HTML shall render retained overview-image assets through webview-safe asset URIs. | Concentrating VI Comparison Report metadata without the corresponding retained overview images leaves the dashboard short of the first-class review surface the product is aiming for. | When a dashboard entry retains overview-image assets, the dashboard HTML contains the overview-image section and uses webview-safe URIs for those retained image assets. | Unit test |
| VHS-REQ-269 | The comparison-report action shall return a stable `cancelled` result with the `before-preflight` stage when cancellation is observed after revision-pair resolution but before revision preflight begins. | Users need a truthful early-stage boundary when they cancel after selecting a retained pair but before any VI revision validation has run. | When cancellation is observed after revision-pair resolution and before preflight, the action returns `cancelled` with stage `before-preflight`, does not run preflight, and does not open a panel. | Unit test |
| VHS-REQ-270 | The comparison-report action shall return a stable `cancelled` result with the `after-preflight` stage when cancellation is observed after revision preflight but before runtime selection begins. | Preflight can retain materially different facts from runtime selection, so the cancellation stage must distinguish “validated revisions” from “selected runtime.” | When cancellation is observed after preflight and before runtime selection, the action returns `cancelled` with stage `after-preflight`, does not select a runtime, and does not open a panel. | Unit test |
| VHS-REQ-271 | The comparison-report action shall return a stable `cancelled` result with the `before-revision-pair-resolution` stage when cancellation is already requested before the action begins resolving the selected retained pair. | The earliest cancellation boundary needs its own governed stage so the action does not imply any revision lookup or preflight work occurred when the request was already cancelled. | When the cancellation token is already requested at action start, the action returns `cancelled` with stage `before-revision-pair-resolution`, does not preflight, and does not open a panel. | Unit test |
| VHS-REQ-272 | The comparison-report archive subsystem shall stamp archived source records with a governed ISO-8601 UTC timestamp when no explicit archive clock override is provided. | Pair-scoped report archives need a truthful retained archival time even in the default runtime path, not only in dependency-injected tests. | Archiving a comparison-report source record without a custom `now` function yields an `archivedAt` value that parses as a UTC ISO-8601 timestamp. | Unit test |
| VHS-REQ-273 | The comparison-report archive planner shall fail closed when the retained archive storage root is blank after trimming. | A pair-scoped archive plan rooted in blank storage is malformed and should be rejected explicitly instead of producing a misleading filesystem path. | Building the archive plan from a retained packet record with a blank first `allowedLocalRootPaths` entry throws `storageRoot must be non-empty`. | Unit test |
| VHS-REQ-274 | The comparison-report archive planner shall default runtime diagnostic-log and process-observation archive filenames from the governed artifact plan when explicit archive filename overrides are absent. | The dashboard archive contract should not rely on callers restating the default runtime artifact names, because that duplicates governed planning knowledge and increases drift risk. | Building the archive plan from a selection with no explicit runtime diagnostic or process-observation filename overrides yields `runtime-diagnostic-log.txt` and `runtime-process-observation.json` under the pair archive root. | Unit test |
| VHS-REQ-275 | The comparison-report archive writer shall skip copying any runtime or report artifact whose source path does not exist while still retaining the pair-scoped source record and other existing artifacts. | Pair archives need to remain usable when some optional runtime artifacts are absent, but the archive writer should not invent placeholder files or fail the whole archive pass. | Archiving a report source with missing runtime artifact files still writes the source record and copied existing packet/report artifacts while leaving the missing runtime artifact paths absent from the archive. | Unit test |
| VHS-REQ-276 | The canonical comparison-report smoke runner shall treat a requested runtime-engine override that already matches the retained runtime engine as a no-op and preserve the original runtime-selection object. | An idempotent override request should not mutate provider/engine facts or append redundant notes, because that would blur whether the runtime choice actually changed. | Requesting `labview-cli` when the retained runtime selection is already `labview-cli` returns the original selection object unchanged. | Unit test |
| VHS-REQ-277 | The canonical comparison-report smoke runner shall fail closed with `requested-labview-cli-not-available` when a `labview-cli` engine override is requested but no governed LabVIEW CLI executable is available on the active host. | An explicit parity override must not silently keep or invent a LabVIEW CLI path when the requested engine is unavailable. | Requesting `labview-cli` with no retained `labviewCli` selection yields `provider: unavailable`, `engine: undefined`, and `blockedReason: requested-labview-cli-not-available`. | Unit test |
| VHS-REQ-278 | The Windows interop command planner shall preserve additional `lvcompare` comparison flags after rewriting the staged VI pair and governed `-lvpath` into Windows-native form. | The non-Windows-host Windows runtime lane should not drop valid `lvcompare` comparison flags while translating the staged pair into Windows paths, because that would change the semantics of the governed parity probe. | Building a Windows interop command plan for `lvcompare` preserves additional flags such as `-nobdcosm` and `-nofp` after rewriting the staged pair and `-lvpath`. | Unit test |
| VHS-REQ-279 | The Windows interop command planner shall fail closed when the retained runtime engine token is unsupported instead of inventing a Windows command plan. | Interop execution should only proceed for governed runtime engines, because an unsupported token would blur whether the resulting Windows command is still under repo control. | Building a Windows interop command plan with an unsupported runtime engine returns `undefined`. | Unit test |
| VHS-REQ-280 | The Windows interop command planner shall fail closed when `lvcompare` staged VI paths or the governed `-lvpath` cannot be normalized into Windows interop form. | The non-Windows-host Windows runtime lane should not emit a partially translated `lvcompare` command, because that would misstate whether the parity probe is still governed and executable on the active host. | Building a Windows interop `lvcompare` command plan returns `undefined` when either staged VI path or the supplied `-lvpath` is not Windows-interop normalizable. | Unit test |
| VHS-REQ-281 | The Windows interop command planner shall rewrite governed `labview-cli` `-VI1`, `-VI2`, `-ReportPath`, and `-LabVIEWPath` values into Windows-native interop form while preserving other command tokens. | The non-Windows-host Windows report lane needs direct proof that the governed CLI command is translated into Windows form without altering its semantic flags. | Building a Windows interop command plan for `labview-cli` rewrites the staged VI paths, report path, and `-LabVIEWPath` into Windows-native form while preserving `-OperationName CreateComparisonReport`. | Unit test |
| VHS-REQ-282 | The Windows interop command planner shall fail closed when an `lvcompare` interop command plan has fewer than two staged VI args. | A Windows interop `lvcompare` plan with an incomplete staged pair is malformed and should not proceed into host execution. | Building a Windows interop `lvcompare` command plan with fewer than two args returns `undefined`. | Unit test |
| VHS-REQ-283 | The Windows interop command planner shall fail closed when a governed `labview-cli` `-ReportPath` or `-LabVIEWPath` value cannot be normalized into Windows interop form. | The non-Windows-host Windows report lane should not emit a partially translated LabVIEW CLI command, because that would blur whether the retained NI invocation is still governed and runnable on the active host. | Building a Windows interop `labview-cli` command plan returns `undefined` when the report path or supplied `-LabVIEWPath` is not Windows-interop normalizable. | Unit test |
| VHS-REQ-284 | The Windows interop command planner shall fail closed when governed `labview-cli` staged `-VI1` or `-VI2` paths cannot be normalized into Windows interop form. | The non-Windows-host Windows report lane should not emit a partially translated LabVIEW CLI command when either staged VI path cannot be represented in Windows form. | Building a Windows interop `labview-cli` command plan returns `undefined` when the staged left or right VI path is not Windows-interop normalizable. | Unit test |
| VHS-REQ-285 | The default runtime executor shall use raw command execution for the Windows-container provider and observation-enabled execution for non-container providers. | The executor’s default runner selection is a governed behavior boundary, because Windows-container runs do not use host-side process observation while host-native and interop runs do. | Building the default run-command helper routes `windows-container` through raw command execution and routes non-container providers through observation-enabled command execution with the governed observation options. | Unit test |
| VHS-REQ-286 | The concentrated dashboard shall retain the `archived-no-generated-report` evidence state when an archived pair record exists but no generated comparison report was retained and the pair was neither blocked nor failed. | The dashboard needs an explicit governed state for archived pairs that did run through the packet/archive flow but still produced no generated report, instead of collapsing them into missing, blocked, or failed states. | Building the concentrated dashboard from an archived pair with `runtimeExecutionState: succeeded` and `reportExists: false` retains `archived-no-generated-report` on the entry and summary evidence-state counts. | Unit test |
| VHS-REQ-287 | The concentrated dashboard shall stamp `generatedAt` with a governed ISO-8601 UTC timestamp when no explicit dashboard clock override is provided. | The default dashboard build path needs a truthful retained generation time even outside dependency-injected tests, so future sessions and users can trust when the concentration packet was produced. | Building the concentrated dashboard without a custom `now` function yields a `generatedAt` value that parses as a UTC ISO-8601 timestamp. | Unit test |
| VHS-REQ-288 | The concentrated dashboard shall retain the `archived-blocked` evidence state and blocked pair summary when an archived pair was blocked before or during runtime execution. | Dashboard reviewers need blocked comparison-report gaps to remain explicit inside the concentrated window summary instead of being merged into generic missing or failed states. | Building the concentrated dashboard from an archived pair with `reportStatus: blocked-runtime` and `runtimeExecutionState: not-available` retains `archived-blocked` on the entry and increments the blocked pair summary fields. | Unit test |
| VHS-REQ-289 | The concentrated dashboard shall skip copying a parsed overview image when the retained source image asset is no longer present on disk. | Dashboard rebuilds should remain deterministic when archived HTML still references an image that has since gone missing, rather than failing the whole concentration pass or inventing a placeholder asset. | Building the concentrated dashboard with parsed overview metadata and a missing retained source image leaves the entry overview counts intact while omitting the copied dashboard image asset. | Unit test |
| VHS-REQ-290 | The history-panel command flow shall fail closed with `unsupported-command` when the panel requests dashboard generation but no dashboard action is wired. | An unwired dashboard handler should not silently noop or imply that dashboard concentration ran; the command layer needs an explicit retained outcome for future sessions and users. | When the panel dispatches `openDashboard` and no dashboard action is configured, the tracker retains `unsupported-command` with no warning or informational message side effects. | Unit test |
| VHS-REQ-291 | The comparison-report packet HTML shall omit the runtime-doctor section when no retained runtime-doctor summary lines are present. | Packet HTML should not render an empty runtime-doctor shell, because that would imply additional troubleshooting guidance exists when no retained doctor summary was actually produced. | Rendering a comparison-report packet with no `doctorSummaryLines` omits the `comparison-report-runtime-doctor` section entirely. | Unit test |
| VHS-REQ-292 | The concentrated dashboard HTML shall render actual NI comparison-report metadata as labeled fields for report title, generation time, compared VI paths, and retained per-pair metadata counts. | The dashboard should act as a coherent metadata concentration surface for expert review, not a compressed status string that forces the reader to reconstruct the NI report fields mentally. | When a retained NI report is present, the dashboard HTML renders labeled report title, generation time, first/second VI paths, and overview/detail count fields for that pair. | Unit tests |
| VHS-REQ-293 | The concentrated dashboard chronology section shall render selected subject, base subject, and pair evidence state for each adjacent retained pair in newest-first order. | Expert review depends on understanding the sequence of adjacent pairs, not only hash identity or aggregate counts. | Dashboard chronology items render selected subject, base subject, and evidence state for each retained pair in newest-first order. | Unit tests |
| VHS-REQ-294 | The concentrated dashboard HTML shall include a metadata-first review lens statement that explains the dashboard concentrates retained NI comparison-report metadata before individual pair-report inspection. | The dashboard should clearly orient expert users toward its intended review mode so it reduces wear from opening many pairwise reports one by one. | The dashboard HTML includes a retained review-lens section describing chronology-first metadata concentration as the primary review surface. | Unit tests |
| VHS-REQ-295 | The repo shall provide a dedicated canonical dashboard smoke lane that retains `dashboard-smoke.json`, `dashboard-smoke.md`, and `dashboard-smoke.html` under `.cache/harness-reports/HARNESS-VHS-001/`. | The dashboard needs the same retained proof surface that history smoke and comparison-report smoke already have, otherwise dashboard claims are hard to verify later. | Running the canonical dashboard smoke command retains the three dashboard-smoke artifacts under the canonical harness report directory. | Unit tests and smoke run |
| VHS-REQ-296 | The canonical dashboard smoke lane shall build the concentrated dashboard from a bounded retained commit window of at least three commits and archive-backed adjacent pair evidence. | Dashboard smoke should prove real multi-pair concentration, not only that an empty dashboard renderer can write files. | The canonical dashboard smoke report records a three-commit-or-larger window, at least two adjacent pairs, and retained archived-pair plus metadata-pair counts. | Unit tests and smoke run |
| VHS-REQ-297 | The extension-host lane shall retain dashboard panel tracking separately from history-panel tracking, including rendered HTML, dashboard paths, pair counts, and dashboard message dispatch. | Once the dashboard becomes a first-class review surface, the real extension-host proof lane needs direct access to that panel instead of inferring behavior through the history panel alone. | The extension API exposes the last opened dashboard panel summary and a dispatcher for the last dashboard panel message handler. | Integration test |
| VHS-REQ-298 | The extension-host lane shall prove governed dashboard artifact-open behavior for retained `packet-html` and `metadata-json` artifacts from the real dashboard panel. | Unit tests alone are not enough to show that the live extension panel can route concentrated dashboard artifacts into the correct VS Code review surfaces. | The real extension-host suite dispatches dashboard artifact messages and retains `opened-artifact-panel` for `packet-html` plus `opened-artifact-editor` for `metadata-json`. | Integration test |
| VHS-REQ-299 | The extension-host lane shall prove that repeated `openDashboard` requests rebuild and reopen the dashboard as a refresh action instead of silently nooping. | A first-class dashboard needs a real refresh path for reviewers after additional report evidence is generated. | Dispatching `openDashboard` twice through the real history panel increases the retained dashboard open count and keeps the second outcome on `opened-review-dashboard`. | Integration test |
| VHS-REQ-300 | The repo entrypoints and research-control-plane status surfaces shall describe live dashboard smoke and extension-host dashboard proof as implemented evidence, not future-only intent. | Future sessions and future LLMs need repo-native status surfaces that reflect actual dashboard proof without reconstructing it from local smoke artifacts. | README, current-state, research alignment, and research implementation index all name the live dashboard smoke lane and extension-host dashboard proof. | Documentation review |
| VHS-REQ-301 | The concentrated dashboard shall retain whole-window overview-caption concentration summaries with caption, represented pair count, and total overview-image count derived from retained VI Comparison Report metadata. | Expert reviewers need one window-level view of recurring overview sections across multiple adjacent reports, not only per-pair caption lists. | Building a dashboard with multiple metadata-backed pairs yields a retained overview-caption concentration summary such as `Front Panel Overview` with pair-count and image-count totals. | Unit tests |
| VHS-REQ-302 | The concentrated dashboard shall retain whole-window included-attribute concentration summaries with attribute label and per-window included versus excluded pair counts. | Reviewers need to see which included-attribute settings recur or diverge across the whole commit window without opening every pair section. | Building a dashboard with multiple metadata-backed pairs yields retained included-attribute concentration summaries such as `Front Panel` with included/excluded pair counts. | Unit tests |
| VHS-REQ-303 | The concentrated dashboard shall retain whole-window detailed-information heading concentration summaries with heading, represented pair count, and total detail-item count. | Repeated detailed-information headings across the commit window are part of the NI metadata surface and should be concentrated for expert review. | Building a dashboard with multiple metadata-backed pairs yields retained detailed-information heading concentration summaries such as `1. VI Attribute - Miscellaneous` with pair-count and item-count totals. | Unit tests |
| VHS-REQ-304 | The concentrated dashboard HTML and dashboard webview shall render the whole-window overview-caption, included-attribute, detailed-information heading, compared-VI-path, and detailed-information item concentration sections ahead of the per-pair sections as the primary metadata-first review layer. | A developer dashboard should reduce the wear of opening individual pair reports by presenting whole-window metadata concentration first. | The rendered dashboard HTML and dashboard panel expose labeled whole-window concentration sections for overview captions, included attributes, detail headings, compared paths, and detail items before the per-pair details. | Unit tests |
| VHS-REQ-305 | The eligibility indexer progress lane shall be cancellable and shall preserve the previous eligibility snapshot when cancellation is requested mid-refresh. | Users should be able to stop long-running eligibility refreshes without replacing a previously known-good eligibility map with partial results. | Cancelling a refresh after progress begins leaves the prior eligible-path context and indexed-repository snapshot intact. | Unit tests |
| VHS-REQ-306 | The eligibility indexer shall fail closed and clear the eligible-path context when workspace trust is lost during an in-flight refresh. | Continued indexing after trust loss would violate the trust boundary and could leave stale eligibility results visible in an untrusted workspace. | If workspace trust becomes false during refresh, the indexer stops, clears its in-memory eligibility snapshot, and resets `labviewViHistory.eligiblePaths` to `{}`. | Unit tests |
| VHS-REQ-307 | The repo shall provide a governed review-scenario registry with at least one active canonical scenario linked to `HARNESS-VHS-001`, its repository URL, its VI target path, and minimum commit-window plus comparison-pair contracts. | The dashboard should act as a decision-support system with explicit scenario contracts, not as an unbounded generic UI surface. | The review-scenario registry exposes `SCENARIO-VHS-001` as active with canonical harness, repository, VI path, and minimum commit/pair requirements. | Unit tests |
| VHS-REQ-308 | Scenario-driven decision-record generation shall fail closed when the retained dashboard evidence does not satisfy the selected scenario contract. | A decision record should not be created from evidence that does not actually meet the scenario’s declared review bounds. | Attempting to create a canonical decision record from fewer than three commits or fewer than two comparison pairs throws a scenario-contract mismatch instead of writing an artifact. | Unit tests |
| VHS-REQ-309 | The repo shall persist human decision records as separate artifacts from the dashboard packet, under a decision-record storage path derived from the same repository, file, and commit-window identity. | Evidence and judgment must remain distinct so machine-produced dashboard packets are not overwritten by human review outcomes. | Decision-record generation writes `decision-record.json` and `decision-record.md` under a `decision-records/<repoId>/<fileId>/<windowId>/<scenarioId>/...` directory instead of mutating the dashboard packet. | Unit tests |
| VHS-REQ-310 | A persisted decision record shall prefill scenario metadata, dashboard artifact paths, underlying pairwise report paths, and retained missing-or-blocked facts from the dashboard evidence pack. | Human reviewers should not have to reconstruct the evidence set manually before recording a bounded decision. | Creating a decision record retains the scenario id/title, dashboard JSON/HTML paths, pairwise report paths, and missing-or-blocked evidence facts in the record body. | Unit tests |
| VHS-REQ-311 | The repo shall provide a canonical harness decision-record command that produces a separate human decision record from the canonical dashboard smoke lane using explicit reviewer inputs. | A governed scenario flow needs a reproducible entrypoint for creating retained decision records against canonical dashboard evidence. | Running the decision-record CLI with reviewer, question, outcome, confidence, and rationale creates a separate retained decision-record artifact for the canonical harness. | Unit tests and local harness run |
| VHS-REQ-312 | Repo-native status surfaces shall describe the review-scenario registry and separate decision-record path as partially implemented evidence rather than future-only modeling once the first canonical scenario flow exists. | Future sessions and future LLMs need the repo entrypoints to reflect that `TRANCHE-007` has started in code, not only in planning docs. | README, current-state, issue tracking, research alignment, and research implementation index all describe the canonical scenario registry and retained decision-record path as implemented in part. | Documentation review |
| VHS-REQ-313 | The repo shall retain one authoritative ship target document that names the active SemVer release target, the current package baseline, the target VSIX artifact, the target release manifest, the stop rule, and the single active ship tranche. | Future sessions should not infer the ship target from scattered chat or issue context; the repo itself must carry the release objective and its bounds. | `SHIP-0001` names `v0.2.0`, current package baseline `0.1.0`, target VSIX `vi-history-suite-0.2.0.vsix`, target release manifest path, and `TRANCHE-009` as the active ship tranche. | Unit tests and documentation review |
| VHS-REQ-314 | The repo shall retain one machine-readable release-readiness matrix that links the ship id, active tranche id, active issue id, current package baseline, target release artifact identity, and per-criterion evidence, blocker, and next action. | Release direction should be enforceable from one structured control-plane file instead of loose prose spread across multiple docs. | `release-readiness-matrix.json` contains `SHIP-0001`, `TRANCHE-009`, `ISSUE-0406`, current package version, target VSIX/manifest identity, and unique readiness criteria with evidence plus next actions. | Unit tests |
| VHS-REQ-315 | The repo shall retain one machine-readable blocker ledger that maps every open ship blocker to a readiness criterion, the active tranche, and the active ship issue. | Release blockers should be traceable directly to the criteria they prevent from reaching done, otherwise iteration can drift away from actual ship risk. | `blocker-ledger.json` records open blockers with unique ids, direct criterion references, `TRANCHE-009`, and `ISSUE-0406`. | Unit tests |
| VHS-REQ-316 | `development-queue.json` shall retain exactly one active tranche, and that active tranche shall match the ship target and the release-readiness matrix. | Multiple active tranches reintroduce drift and make it unclear which work is actually paying down the ship target. | The development queue contains only one `active` row, and its id equals the active tranche in both `SHIP-0001` and `release-readiness-matrix.json`. | Unit tests |
| VHS-REQ-317 | Repo entrypoint, information-item-map, program, and release-procedure surfaces shall point readers to the ship target, release-readiness matrix, blocker ledger, and release procedure as the primary direction surfaces for remaining ship work. | Human readers and future LLMs should find the release objective from the repo entrypoints immediately instead of reconstructing it from implementation details. | README, current-state, information item map, `PROGRAM-0001`, and release procedure all point to the ship-control surfaces, while README and current-state also surface the blocker ledger and release artifact identity. | Unit tests and documentation review |
| VHS-REQ-318 | The repo shall provide a governing validation test that proves the ship target, development queue, package baseline, release-readiness matrix, blocker ledger, and entrypoint docs remain aligned. | The ship-control system only prevents drift if the repo can fail fast when one surface falls behind the others. | The ship-control unit suite fails when the queue has multiple active tranches, the package version drifts from the readiness matrix baseline, blocker ids no longer line up with non-done criteria, or the entrypoint docs stop referencing the control-plane surfaces. | Unit tests |
| VHS-REQ-319 | The GitLab release lane shall fail closed when a SemVer tag does not match the package version declared in `package.json`. | Shipping the wrong package under the wrong tag would break exact-version installation and undermine the release evidence. | The release job compares `CI_COMMIT_TAG` to `package.json` and exits nonzero when they differ. | Unit tests |
| VHS-REQ-320 | The GitLab release lane shall package a versioned VSIX artifact named `vi-history-suite-<version>.vsix` under `release-evidence/` for SemVer tag pipelines. | `SHIP-0001` requires a concrete installable artifact, not only compile/test evidence. | The release job invokes VSIX packaging with an explicit `release-evidence/vi-history-suite-<version>.vsix` output path. | Unit tests |
| VHS-REQ-321 | The GitLab release lane shall retain a machine-readable `release-manifest.json`, a human-readable release record, and checksum evidence alongside the versioned VSIX artifact. | Users and future sessions need a retained exact-version evidence pack that identifies the artifact, commit, and supporting evidence without reconstructing the release from pipeline logs. | The release job writes `release-evidence/release-manifest.json`, `release-evidence/release-record.md`, and `release-evidence/vi-history-suite-<version>.vsix.sha256` with the SemVer tag, package version, commit, VSIX identity, and evidence paths. | Unit tests and documentation review |
| VHS-REQ-322 | The GitLab `main` pipeline shall retain a preview VSIX artifact and preview manifest so extension users can install the latest governed build before the first tagged release is retained. | Delivery should be useful before the formal SemVer release criterion is fully closed, otherwise the pipeline cannot be leveraged for real extension-user testing. | A successful `main` pipeline retains `preview-evidence/vi-history-suite-<version>.vsix` and `preview-evidence/preview-manifest.json`. | Unit tests and documentation review |
| VHS-REQ-323 | Repo-native release surfaces shall distinguish preview-install artifacts from the governed tagged-release artifact so users do not confuse pre-release install testing with formal ship evidence. | A preview lane is valuable, but it should not blur the line between install testing and the exact-version release gate. | README, current state, release procedure, readiness matrix, and blocker ledger all describe preview VSIX artifacts as install surfaces while keeping the tagged release artifact as the formal ship criterion. | Unit tests and documentation review |
| VHS-REQ-324 | The eligibility indexer progress message shall keep the processed-versus-total count truthful for the repository named in the active progress message and shall not overrun that repository’s tracked-file total. | Indexing feedback becomes misleading if the active repository name is paired with a processed count accumulated from earlier repositories. | Refreshing multiple repositories emits progress messages such as `repo-a 2/2` followed by `repo-b 1/1`, never `repo-b 3/1` or similar cross-repository overruns. | Unit tests |
| VHS-REQ-325 | The live comparison-report panel shall render the retained NI-generated HTML report directly when a generated report exists, while preserving the governed packet artifact separately on disk. | Expert users need the actual NI HTML report, including its sibling `_files` assets, to render reliably in the extension panel instead of being nested through a packet frame that can break image resolution. | A succeeded comparison-report action opens a panel that injects the retained NI report HTML into the live webview with a webview-safe base URI for sibling assets, while blocked or no-report cases continue to open the governed packet artifact. | Unit tests |
| VHS-REQ-326 | The eligibility indexer shall limit repository discovery and repository-state listeners to Git repositories that are relevant to the current workspace scope. | Extension users who open one repository should not have indexing jump across unrelated Git repositories merely because the built-in Git extension surfaced them. | Repository discovery and repository listener registration ignore Git repositories outside the current workspace roots, while still allowing workspace folders nested inside a Git working tree to resolve to their containing repository root. | Unit tests |
| VHS-REQ-327 | The eligibility indexer shall coalesce refresh requests that arrive while a refresh is already in flight into at most one additional pass after the current pass completes. | Git state-change storms can otherwise retrigger repeated overlapping or back-to-back indexing passes that look unstable to users. | Triggering refresh during an active refresh does not start a concurrent pass; instead the indexer performs one follow-up pass after the active pass finishes. | Unit tests |
| VHS-REQ-328 | The `diffPrevious` action shall open retained comparison-report evidence for content-detected LabVIEW VI revisions when governed retained pair evidence is available, instead of invoking VS Code’s text diff on binary VI content. | Expert LabVIEW users need a viable comparison surface for binary VIs, and the built-in text diff editor reports the content as unsupported instead of showing useful differences. | Dispatching `diffPrevious` for an `LVIN` or `LVCC` item with retained comparison-report support records an opened comparison-report outcome with retained report metadata instead of executing `vscode.diff`. | Unit and integration tests |
| VHS-REQ-329 | The `diffPrevious` action shall fail closed with `missing-retained-comparison-report` and a stable informational message that directs the user to `Generate compare` when retained pair evidence does not yet exist. | Opening retained comparison evidence should not silently regenerate or imply that a retained NI report already exists when it does not. | Dispatching `diffPrevious` for a content-detected VI without retained pair evidence records `missing-retained-comparison-report` and surfaces an informational message directing the user to `Generate compare`. | Unit and integration tests |
| VHS-REQ-330 | The concentrated dashboard shall retain whole-window compared-VI-path concentration summaries with first-path, second-path, and represented pair count derived from retained NI comparison-report metadata. | Expert reviewers need to see whether the same compared VI path pair recurs across the commit window without reading every per-pair section. | Building a dashboard with multiple metadata-backed pairs yields retained compared-path concentration summaries with the compared path pair and represented pair count. | Unit tests |
| VHS-REQ-331 | The concentrated dashboard shall retain whole-window detailed-information item concentration summaries with the retained item text and represented pair count derived from retained NI comparison-report metadata. | Repeated detail items across the commit window are part of the NI metadata surface and should be concentrated for expert review rather than left only inside per-pair sections. | Building a dashboard with multiple metadata-backed pairs yields retained detail-item concentration summaries with the item text and represented pair count. | Unit tests |

## Assumptions

- VS Code desktop with Node extension host is the first target runtime.
- Git is available for CLI-backed history operations.
- Live NI report generation and dashboard concentration are active product
  surfaces, with remaining runtime-proof and dashboard drill-down work tracked
  in the development queue.

## Constraints

- public repository with restrictive source-available licensing
- sole-author maintenance model
- no external contribution intake by default
