{
  "name": "vi-history-suite",
  "displayName": "VI History Suite",
  "description": "VS Code extension for content-detected LabVIEW VI history review in Git repositories.",
  "version": "1.36.1",
  "publisher": "svelderrainruiz",
  "license": "0BSD",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/LabVIEW-Community-CI-CD/vi-history-suite.git"
  },
  "homepage": "https://github.com/LabVIEW-Community-CI-CD/vi-history-suite#readme",
  "bugs": {
    "url": "https://github.com/LabVIEW-Community-CI-CD/vi-history-suite/issues"
  },
  "engines": {
    "vscode": "^1.101.0"
  },
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "VI History disables selected-file history evaluation and comparison execution in untrusted workspaces to prevent external process execution. Documentation and local runtime settings CLI preparation remain available.",
      "restrictedConfigurations": [
        "viHistorySuite.runtimeProvider",
        "viHistorySuite.labviewVersion",
        "viHistorySuite.labviewBitness",
        "viHistorySuite.labviewExePath",
        "viHistorySuite.labviewCliPath",
        "viHistorySuite.container.imageVersion"
      ]
    }
  },
  "categories": [
    "Other",
    "SCM Providers"
  ],
  "icon": "resources/marketplace/vi-history-suite-icon.png",
  "main": "./out/extension.js",
  "files": [
    "out/**",
    "node_modules/jsonc-parser/**",
    "resources/**",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "mcpServerDefinitionProviders": [
      {
        "id": "viHistorySuiteSemantic",
        "label": "VI History Suite: VI Semantic Comparison"
      }
    ],
    "commands": [
      {
        "command": "labviewViHistory.open",
        "title": "Review VI History",
        "category": "VI History"
      },
      {
        "command": "labviewViHistory.openDocumentation",
        "title": "Open Documentation",
        "category": "VI History"
      },
      {
        "command": "labviewViHistory.copyReviewPacket",
        "title": "Copy Review Details",
        "category": "VI History"
      },
      {
        "command": "labviewViHistory.prepareLocalRuntimeSettingsCli",
        "title": "Set Up Comparison Runtime",
        "category": "VI History"
      },
      {
        "command": "labviewViHistory.detectRuntimeNow",
        "title": "Detect Runtime Now",
        "category": "VI History"
      },
      {
        "command": "labviewViHistory.resetFirstRunNotice",
        "title": "Reset First-Run Runtime Notice",
        "category": "VI History"
      },
      {
        "command": "labviewViHistory.showRuntimeSummary",
        "title": "Show Runtime Summary",
        "category": "VI History"
      },
      {
        "command": "labviewViHistory.pickRuntimeProvider",
        "title": "Runtime & Report Settings",
        "category": "VI History"
      },
      {
        "command": "labviewViHistory.exportComparisonReport",
        "title": "Export Comparison Report (HTML)",
        "category": "VI History",
        "icon": "$(save-as)"
      },
      {
        "command": "labviewViHistory.openViHistoryFromReport",
        "title": "Open VI History",
        "category": "VI History",
        "icon": "$(history)"
      },
      {
        "command": "labviewViHistory.installPinnedDevTools",
        "title": "Install Pinned Dev-Tools Version",
        "category": "VI History"
      },
      {
        "command": "labviewViHistory.uninstallDevTools",
        "title": "Uninstall Dev-Tools Version",
        "category": "VI History"
      },
      {
        "command": "labviewViHistory.showDevToolsStatus",
        "title": "Show Dev-Tools Status",
        "category": "VI History"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "labviewViHistory.open",
          "group": "3_compare",
          "when": "(resourceExtname == .vi || resourceExtname == .ctl || resourceExtname == .vit) && isWorkspaceTrusted"
        }
      ],
      "editor/title/context": [
        {
          "command": "labviewViHistory.open",
          "group": "3_compare",
          "when": "(resourceExtname == .vi || resourceExtname == .ctl || resourceExtname == .vit) && isWorkspaceTrusted"
        }
      ],
      "editor/title": [
        {
          "command": "labviewViHistory.openViHistoryFromReport",
          "group": "navigation",
          "when": "activeWebviewPanelId == viHistorySuite.comparisonReport"
        },
        {
          "command": "labviewViHistory.exportComparisonReport",
          "group": "navigation",
          "when": "activeWebviewPanelId == viHistorySuite.comparisonReport"
        }
      ],
      "commandPalette": [
        {
          "command": "labviewViHistory.openViHistoryFromReport",
          "when": "activeWebviewPanelId == viHistorySuite.comparisonReport"
        },
        {
          "command": "labviewViHistory.exportComparisonReport",
          "when": "activeWebviewPanelId == viHistorySuite.comparisonReport"
        }
      ]
    },
    "customEditors": [
      {
        "viewType": "viHistorySuite.viPreview",
        "displayName": "VI History Preview",
        "selector": [
          {
            "filenamePattern": "*.vi"
          },
          {
            "filenamePattern": "*.vit"
          },
          {
            "filenamePattern": "*.vim"
          },
          {
            "filenamePattern": "*.ctl"
          }
        ],
        "priority": "default"
      }
    ],
    "configuration": {
      "title": "VI History Suite",
      "properties": {
        "viHistorySuite.strictRsrcHeader": {
          "type": "boolean",
          "default": false,
          "description": "Use stricter checks when deciding whether a file is a real LabVIEW VI. Leave this off unless files are being wrongly accepted or rejected."
        },
        "viHistorySuite.historyWindowMode": {
          "type": "string",
          "enum": [
            "auto",
            "capped"
          ],
          "default": "auto",
          "description": "How many past versions to load. Auto shows the full history when it is a manageable size; capped limits it to the number set in Max History Entries."
        },
        "viHistorySuite.maxHistoryEntries": {
          "type": "number",
          "default": 100,
          "minimum": 2,
          "maximum": 5000,
          "description": "The most past versions to show when History Window Mode is set to capped."
        },
        "viHistorySuite.runtimeProvider": {
          "type": "string",
          "enum": [
            "host",
            "docker"
          ],
          "description": "Where comparisons run: Host uses the LabVIEW installed on your machine (default); Docker uses an NI LabVIEW container image. Most people leave this on Host."
        },
        "viHistorySuite.labviewVersion": {
          "type": "string",
          "description": "Which LabVIEW year to use for comparisons (for example 2025 or 2026). LabVIEW 2025 and newer can open older VIs without converting them first."
        },
        "viHistorySuite.labviewBitness": {
          "type": "string",
          "enum": [
            "x86",
            "x64"
          ],
          "description": "Whether to use 32-bit (x86) or 64-bit (x64) LabVIEW for comparisons. Match this to the LabVIEW you have installed."
        },
        "viHistorySuite.labviewExePath": {
          "type": "string",
          "description": "Optional: the full path to your LabVIEW program (LabVIEW.exe on Windows, labview on Linux). Set this only if VI History cannot find LabVIEW automatically."
        },
        "viHistorySuite.labviewCliPath": {
          "type": "string",
          "description": "Optional: the full path to the LabVIEW CLI (LabVIEWCLI). Set this only if VI History cannot find it automatically; this also lets VI History open when the LabVIEW CLI cannot be detected."
        },
        "viHistorySuite.container.imageVersion": {
          "type": "string",
          "description": "Which LabVIEW Docker image to use for Docker comparisons (for example 2026q1-windows). Leave empty to use the newest supported image. Run the VI History: Runtime & Report Settings command to pick from images available on Docker Hub or already downloaded; new National Instruments patches and years appear automatically."
        },
        "viHistorySuite.preview.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Turn on VI Preview (Docker runtime only): opening a LabVIEW VI (.vi/.vit/.vim/.ctl) renders it as a read-only picture of its front panel and block diagram, and the extension silently caches every VI in the repository in the background. Off by default. Manage this from the VI History: Runtime & Report Settings command — the toggle appears there when Docker is selected."
        },
        "viHistorySuite.preview.backgroundWarming": {
          "type": "string",
          "enum": [
            "docker-only",
            "always",
            "off"
          ],
          "enumDescriptions": [
            "Only pre-render in the background when comparisons run in Docker, so your installed LabVIEW is never tied up. Recommended default.",
            "Also pre-render in the background when using your installed (Host) LabVIEW, so you see caching progress on Host too. This keeps LabVIEW busy while it caches a large workspace.",
            "Never pre-render previews in the background."
          ],
          "default": "docker-only",
          "description": "After you open a VI preview, whether VI History quietly pre-renders the other VIs in the workspace so they open instantly, shown as a caching percentage in the status bar. Docker-only (default) skips this when using Host so your LabVIEW stays free; choose Always to see caching progress on Host too."
        },
        "viHistorySuite.preview.blockDiagramInteractive": {
          "type": "boolean",
          "default": false,
          "description": "Show the VI block diagram as an interactive view instead of a static picture: pan by dragging, zoom with Ctrl/Cmd+scroll, and step through each Case/Event/Sequence structure's cases in place with the ◀ n/N ▶ selector or the arrow keys. Off by default (the static document view is shown). Requires VI Preview to be on."
        },
        "viHistorySuite.preview.allowHostNativeRender": {
          "type": "boolean",
          "default": false,
          "description": "Allow the Host (installed LabVIEW) runtime to render VI previews directly, instead of only displaying previews already cached on Docker. Off by default: Docker generates the cache and the Host displays it. Turn this on for a dedicated LabVIEW environment that has no Docker (for example the Vagrant LabVIEW VM) so it can both generate the cache and visualize previews. Requires VI Preview to be on."
        },
        "viHistorySuite.devTools.version": {
          "type": "string",
          "default": "bundled",
          "pattern": "^(bundled|(devtools-)?v?(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(-[0-9A-Za-z-.]+)?(\\+[0-9A-Za-z-.]+)?)$",
          "markdownDescription": "Which dev-tools build (the compiled MCP server and companion tooling) the extension runs. `bundled` (default) uses the build shipped with the extension and never touches the network. Pin an independent dev-tools release with a `devtools-vX.Y.Z` tag (SemVer 2.0) to run that version without waiting for a Marketplace update; the extension downloads it once into global storage, verifies its integrity, and launches it only in a trusted workspace. Requires a matching published dev-tools release."
        },
        "viHistorySuite.devTools.checkForUpdates": {
          "type": "boolean",
          "default": false,
          "description": "Opt in to a background check that notifies you when a newer stable dev-tools version than the one you have pinned is available. Off by default and never contacts the network unless enabled. Only stable releases are surfaced (prereleases are ignored)."
        },
        "viHistorySuite.preview.warmOnChange": {
          "type": "boolean",
          "default": true,
          "description": "When Docker is selected, automatically cache a VI as soon as you change it on disk so its preview opens instantly and the Source Control change summary is ready. Warms the preview render (requires VI Preview to be on) and runs a background comparison against the last commit (requires a trusted workspace). On by default; only runs on the Docker runtime."
        },
        "viHistorySuite.runtime.cliConnectTimeoutSeconds": {
          "type": "integer",
          "default": 180,
          "minimum": 30,
          "maximum": 600,
          "description": "How long (in seconds) to wait for LabVIEW to start before a comparison gives up. Increase this if the first comparison after opening VS Code times out on a slow machine. Default is 180."
        },
        "viHistorySuite.comparison.worktreeSnapshotRetentionLimit": {
          "type": "integer",
          "default": 5,
          "minimum": 0,
          "description": "How many working-tree (uncommitted) comparison snapshots to keep per VI in the retained dashboard history. Each uncommitted comparison is saved as a content-addressed snapshot; older ones beyond this limit are pruned. Set to 0 to turn off retaining uncommitted comparisons entirely. Default is 5."
        },
        "viHistorySuite.report.ignoreViAttributes": {
          "type": "boolean",
          "default": false,
          "description": "Leave VI attribute changes out of the comparison report."
        },
        "viHistorySuite.report.ignoreFrontPanel": {
          "type": "boolean",
          "default": false,
          "description": "Leave all front-panel changes out of the comparison report."
        },
        "viHistorySuite.report.ignoreFrontPanelObjectPosition": {
          "type": "boolean",
          "default": false,
          "description": "Leave front-panel changes that are only moves or resizes out of the comparison report."
        },
        "viHistorySuite.report.ignoreBlockDiagram": {
          "type": "boolean",
          "default": false,
          "description": "Leave all block-diagram changes out of the comparison report."
        },
        "viHistorySuite.report.ignoreBlockDiagramCosmetic": {
          "type": "boolean",
          "default": false,
          "description": "Ignore cosmetic block-diagram changes (such as moving or resizing items) so the report shows only changes that affect behavior."
        }
      }
    }
  },
  "scripts": {
    "clean": "rimraf out out-tests coverage",
    "prepare": "node scripts/installGitHooks.js && node scripts/checkEnvSync.js --record-lock-hash",
    "hooks:install": "node scripts/installGitHooks.js",
    "env:sync:check": "node scripts/checkEnvSync.js --report --label env",
    "deps:check": "node scripts/checkDevDependencies.js",
    "precompile": "node scripts/checkDevDependencies.js",
    "compile": "tsc -p . && node scripts/generateBuildInfo.js && node scripts/exportRequirementsManifest.js",
    "precheck": "node scripts/checkDevDependencies.js",
    "check": "tsc -p . --noEmit",
    "quality:local": "npm run check && npm test && npm run traceability:audit && npm run customization:audit && npm run docs:links && npm run coverage:map && npm run requirements:verify",
    "branch-protection:audit": "node scripts/auditBranchProtectionSettings.js",
    "repo:truth": "node scripts/readRepoTruth.js",
    "repo:governance:mcp": "node scripts/repoGovernanceMcp.js",
    "control-plane:write": "node scripts/controlPlaneWrite.js",
    "control-plane:board-sync": "node scripts/controlPlaneBoardSync.js",
    "control-plane:digest": "node scripts/renderControlPlaneDigest.js",
    "governance:gates": "node scripts/checkGovernanceGates.js",
    "build-info:check": "node scripts/checkBuildInfoIntegrity.js",
    "customization:audit": "node scripts/auditCustomizationGovernance.js",
    "dev:watch": "tsc -p . --watch --preserveWatchOutput",
    "dev:workspace": "npm run compile && node out/cli/runDevHost.js --prepare-workspace-only",
    "dev:host": "npm run compile && node out/cli/runDevHost.js",
    "public:host:bootstrap-linux": "node scripts/bootstrapLinuxVsCodeHost.js install",
    "public:fixture:icon-editor": "node scripts/preparePublicTestFixture.js",
    "public:repo:clone": "node scripts/preparePublicRepoClone.js",
    "vagrant:validate": "cd vagrant && vagrant validate",
    "vagrant:preflight": "node scripts/vagrantLanePreflight.js preflight",
    "vagrant:status": "node scripts/vagrantLanePreflight.js status",
    "vagrant:validate:release": "node scripts/vagrantReleaseValidate.cjs",
    "pretest": "npm run compile",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "test:integration:compile": "tsc -p tsconfig.integration.json",
    "test:integration": "npm run compile && npm run test:integration:compile && node out-tests/tests/integration/runTests.js",
    "test:integration:linux": "node scripts/runLinuxIntegrationHost.js",
    "test:integration:windows": "node scripts/runWindowsIntegrationHost.js",
    "assurance:state": "node scripts/generateAssuranceState.js",
    "standards:audit": "node scripts/runMultiStandardsAudit.js",
    "adr:check": "node scripts/checkAdrIndex.js",
    "agent:check": "node scripts/checkAgentDelegation.js",
    "issue:standards-triage": "node scripts/runIssueStandardsTriage.js",
    "package:audit": "node scripts/auditPackagedRuntimeSurface.js",
    "package": "npm run compile && npm run package:audit && node scripts/runPinnedVsce.js package",
    "traceability:audit": "node scripts/auditTraceabilitySteward.js",
    "requirements:csv-check": "node scripts/checkRequirementsCsvColumns.js",
    "requirements:integrity": "node scripts/checkRequirementsIntegrity.js",
    "requirements:linkage": "node scripts/auditRequirementVerificationLinkage.js",
    "requirements:linkage:enforce": "node scripts/auditRequirementVerificationLinkage.js --enforce",
    "requirements:criteria": "node scripts/auditRequirementCriteriaInventory.js",
    "requirements:criteria:enforce": "node scripts/auditRequirementCriteriaInventory.js --enforce",
    "requirements:verify": "node scripts/verifyRequirementsHealth.js",
    "requirements:verify:strict": "node scripts/verifyRequirementsHealth.js --strict",
    "requirements:manifest": "node scripts/exportRequirementsManifest.js",
    "risk:ledger": "node scripts/buildRiskLedger.js",
    "risk:ledger:strict": "node scripts/buildRiskLedger.js --strict",
    "runtime:validation:record": "node scripts/recordRuntimeValidation.js",
    "devtools:release": "node scripts/buildDevToolsRelease.js",
    "devtools:release:verify": "node scripts/verifyDevToolsRelease.js",
    "supply-chain:state": "node scripts/buildSupplyChainState.js",
    "release:state": "node scripts/buildReleaseState.js",
    "release:state:strict": "node scripts/buildReleaseState.js --strict",
    "release:readiness": "node scripts/checkReleaseReadiness.js",
    "release:readiness:strict": "node scripts/checkReleaseReadiness.js --strict",
    "release:readiness:gate": "node scripts/checkReleaseReadiness.js --strict --require-release-attestation",
    "verify:preview": "npm run compile && node out/tooling/viPreviewVerifyCli.js",
    "preview:diagnostics": "npm run compile && node out/tooling/viPreviewVerifyCli.js --diagnostics",
    "preview:cache:warm": "npm run compile 1>&2 && node out/cli/runViPreviewCacheWarmer.js",
    "preview:cache:health": "npm run compile 1>&2 && node out/cli/runViPreviewCacheHealth.js",
    "preview:cache:bundle": "npm run compile 1>&2 && node out/cli/runViPreviewCacheBundle.js",
    "preview:cache:exchange": "npm run compile 1>&2 && node out/cli/runViPreviewCacheExchange.js",
    "docs:links": "node scripts/checkDocsLinks.js",
    "docs:gate": "node scripts/checkDocumentationWorkbench.js",
    "coverage:map": "node scripts/mapCoverageToTraceability.js",
    "coverage:map:enforce": "node scripts/mapCoverageToTraceability.js --enforce",
    "test:mutation": "stryker run",
    "dod:gate": "node scripts/checkDefinitionOfDone.js",
    "closeout:evidence": "node scripts/generateCloseoutEvidence.js",
    "validation:file-gap": "node scripts/fileLinuxValidationGap.js"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@types/node": "^24.13.3",
    "@types/vscode": "^1.101.0",
    "@vitest/coverage-v8": "^4.1.10",
    "@vscode/test-electron": "^3.0.0",
    "@vscode/vsce": "3.9.2",
    "marked": "^18.0.6",
    "rimraf": "^6.0.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "jsonc-parser": "^3.3.1"
  },
  "overrides": {
    "qs": "^6.15.3"
  }
}
