{
  "$comment": "VHS-REQ-696 control-plane governed write path. Tier 1 (boardSync) is ENABLED (maintainer-approved 2026-07-20): it mirrors only directly-verified read-model truth (verified closures -> Status Done + Evidence Proven) and needs the enabled flag but not per-action approval. Tiers 2 (annotate), 3 (mergeQueue), and 4 (createWork) are ENABLED (maintainer-approved 2026-07-20) but, unlike boardSync, additionally require a server-verified allowlisted approver per action. Live CI writes additionally require the maintainer-provisioned CONTROL_PLANE_PROJECT_TOKEN secret; the ambient token cannot edit Project #4.",
  "enabled": true,
  "approvers": ["svelderrainruiz"],
  "tokenSource": "local-agent-session",
  "tiers": {
    "boardSync": true,
    "annotate": true,
    "mergeQueue": true,
    "createWork": true
  }
}
