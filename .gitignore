node_modules/
out/
out-tests/
dist/
coverage/
.cov-*/
.vscode-test/
.stryker-tmp/
reports/
*.vsix
*.log
.devcontainer/devcontainer-lock.json
.cache/
tmp/
assurance-*-evidence/

# Windows comparison-runtime validation run evidence (issue #296 driver output)
win-validation/

# Linux comparison-runtime / semantic validation run evidence (driver output)
lin-validation/


# Vagrant acceptance artefacts
vagrant/shared/
vagrant/evidence/
vagrant/.vagrant/
vagrant/.vagrant-ci/

# Offline installer ISOs (never commit)
*.iso

control-plane-write-log.jsonl

# Local comparison-report runtime output (never committed)
/storage/

# Mirror Mode transient fingerprint inputs (left-channel producer)
.mirror-fp/

# Vendored .NET TDMS infrastructure build outputs (tools/tdms, VHS-REQ-710)
tools/**/bin/
tools/**/obj/

# Local lvkit/preview runtime cache (never commit)
.vihs/
